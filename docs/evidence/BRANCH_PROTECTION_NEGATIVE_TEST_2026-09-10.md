# Branch protection negative acceptance test

Purpose: verify that the active `Protect main` ruleset on `arvectum2/proxy-launcher` blocks merging a pull request before the required `build` status check succeeds.

This file is test-only and must not be merged into `main`.
