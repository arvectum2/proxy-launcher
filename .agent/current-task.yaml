version: 2
repository: arvectum2/proxy-launcher
task_id: APL-MAC-RECOVERY-AUTOSTART-20260919
status: in_progress
goal: "Repair macOS rollback/recovery and autostart so Launcher can safely restore the pre-Arvectum PAC state without severing the owner's proxy connectivity."
authority: OWNER_DIRECTIVE
definition_of_done:
  - "macOS rollback correctly restores services whose original PAC URL was empty/disabled without calling networksetup with an invalid empty URL."
  - "A second rollback attempt safely completes a partially restored state when each service is either still Arvectum-owned or already at its saved snapshot."
  - "macOS process ownership does not mistake a foreign compatible localhost PAC listener for the Arvectum engine."
  - "macOS autostart uses the dedicated ownership-safe LaunchAgent implementation and no Windows portable/stable-copy path."
  - "Relevant backend/GUI/process/autostart tests pass."
  - "Owner receives an exact manual recovery command that restores the saved pre-Arvectum PAC state: Ethernet/Wi-Fi enabled on localhost PAC, Thunderbolt/Tailscale disabled, with saved bypass domains."
  - "No live PAC/network mutation is performed by the agent before the owner runs the explicit recovery command."
completed:
  - "Recovered completed macOS UI checkpoint and verified repository state."
  - "Read current rollback evidence and identified saved pre-Arvectum PAC state."
  - "Root cause fixed: disabled+empty PAC snapshots no longer call invalid networksetup -setautoproxyurl with an empty URL."
  - "Rollback is now retry-safe after partial restoration: already-saved services are accepted while remaining owned services are restored; foreign changes still fail closed."
  - "Root cause fixed: macOS is_running now requires an owned PID record and executable-path match instead of trusting any compatible localhost PAC listener."
  - "macOS PID termination now fails closed on executable identity mismatch."
  - "macOS autostart now uses macos_autostart LaunchAgent code instead of Windows Run/schtasks/portable-copy logic."
  - "LaunchAgent enable/disable is ownership-safe and refuses to overwrite/delete a foreign plist."
  - "Read-only live classification confirms Ethernet=SAVED, Wi-Fi=SAVED, Tailscale=OWNED, Thunderbolt Bridge=OWNED."
  - "Confirmed the baseline tunnel-proxy.py is a separate process and current Launcher settings collide with its 8080/1080/8082 ports; post-recovery port separation remains required before enabling Arvectum."
  - "Removed an exact-owned LaunchAgent accidentally created by an early local test; no network/PAC state was changed."
  - "Implementation committed and pushed as 943de63."
  - "Built and verified fixed private arm64 app without installing it; strict codesign PASS, bundle id ru.arvectum.proxylauncher, executable SHA256 b4dfe66fa1a4cf9476ec3f433356869440176765948b1983cabfa24f3c557d9c."
current: "Implementation is committed/pushed and the fixed private arm64 app is built. Await owner execution of the exact saved-state PAC recovery command before installing or changing local ports."
pending:
  - "Owner executes the exact saved-state PAC recovery command and confirms completion."
  - "After owner recovery, reconnect via RDC, move Arvectum to non-conflicting local ports, install fixed app, verify enable/disable/restore/autostart, then complete checkpoint."
last_verified_commit: "943de63ac1b5c12c5c47c464c6287b8dbca9b8c1"
test_results:
  - "Targeted backend/process/GUI/autostart: 42/42 PASS."
  - "Full test_macos*.py discovery: 45/45 PASS."
  - "Broader process/GUI/runtime/core/backend/license suite: 115/115 PASS."
  - "git diff --check PASS.
  - "Implementation commit 943de63ac1b5c12c5c47c464c6287b8dbca9b8c1 pushed to origin/agent/macos-recovery-autostart-20260919."
  - "Private arm64 app build PASS; strict deep codesign PASS; executable SHA256 2e73c93f0a4cd8b18666cbc5da662e18eb77d5b970980270d8d3051e1ebfd925."
resume_instruction: "Owner must first run the provided saved-state PAC recovery command. After confirmation, verify PAC state via RDC, change Arvectum local ports away from baseline 8080/1080/8082, install the already-built fixed app, and run live enable/disable/restore/autostart acceptance."
updated_at: "2026-09-19T02:45:00+03:00"
execution:
  claimed_by: "ChatGPT"
  execution_id: "APL-MAC-RECOVERY-AUTOSTART-20260919"
  branch: "agent/macos-recovery-autostart-20260919"
  base_commit: "87c14fe"
  manual_override: true
