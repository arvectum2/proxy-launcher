version: 2
repository: arvectum2/proxy-launcher
task_id: APL-REDOS-002
status: in_progress
goal: "Install RED OS 8.0.3 as third OS beside Windows and Astra without modifying existing partitions p1-p5."
definition_of_done:
  - "RED OS installed only on p6/p7."
  - "Windows, Astra and RED OS all boot from the unified menu."
  - "ARVECTUM-DEMO remains a low-complexity demo stand; BitLocker is not required and must not be re-enabled as part of this task."
completed:
  - "RED OS 8.0.3 installed on p6/p7; p1-p5 preserved."
  - "RED OS Boot0006 repaired and real reboot test PASS; BootCurrent=0006."
  - "RED OS is running kernel 6.12.101-1.red80.x86_64; previous 6.12.92 retained as rollback kernel plus rescue entry."
  - "Windows and Astra both booted successfully after RED OS installation."
  - "Windows Proxy Launcher 0.2.5 installed, configured and working; obsolete Windows launcher/test artifacts cleaned."
  - "Windows RTC UTC interoperability fixed; BitLocker protection is currently suspended."
  - "RED OS Desktop Commander persistence fixed with ~/.config/systemd/user/desktop-commander-remote.service; sole-connector/ping test PASS."
  - "Removed obsolete RED OS KDE RDC autostart and stale ~/.npm/_npx cache."
  - "Root GRUB/BLS inspection completed. BLS contains current 6.12.101, fallback 6.12.92 and rescue; grubby default-kernel is 6.12.101 and default-index is 0."
  - "Confirmed repaired /boot/efi/EFI/redos/grub.cfg is owned by grub2-efi-x64 and grub2-common scriptlet preserves an already-unified EFI stub."
  - "Backed up /etc/default/grub, /etc/grub.d/40_custom and /boot/grub2/grub.cfg with timestamp 20260916-160707."
  - "Installed explicit chainloader entries for Windows 11 and Astra Linux 1.8 using shared ESP UUID 3EE2-B918; verified both EFI targets exist before regeneration."
  - "Set GRUB_TIMEOUT_STYLE=menu and GRUB_DISABLE_OS_PROBER=true; kept BLS enabled and retained rollback/rescue kernels."
  - "Regenerated /boot/grub2/grub.cfg atomically; grub2-script-check PASS; EFI stub remained unchanged."
  - "User clarified ARVECTUM-DEMO is the disposable/low-data three-OS demo stand, distinct from the Windows development laptop whose user is GN. Do not restore BitLocker on this stand."
  - "Explicit `Astra Linux 1.8` entry from RED OS GRUB runtime test: PASS. Astra booted while UEFI BootCurrent remained 0006 as expected for chainloading from RED OS."
  - "Astra clean GUI boot retest: PASS. fly-dm is enabled+active; Xorg :0 vt7 and fly-wm started automatically; zero failed systemd units."
  - "Astra clock after synchronization: PASS; Europe/Moscow, UTC RTC, NTP active, system clock synchronized."
  - "Astra current manually-started RDC is online/ping PASS. Copied Desktop Commander 0.2.50 from temporary npx cache to permanent ~/.local/share/desktop-commander and staged ~/.local/bin/desktop-commander-remote plus user systemd service for autostart."
current: "Astra is healthy and the unified Astra chainloader is proven. Finish the staged Astra RDC migration by enabling the user service and handing off from the current manual npx connector to the permanent systemd-managed connector without leaving duplicate same-device sessions."
pending:
  - "Enable Astra ~/.config/systemd/user/desktop-commander-remote.service and perform a controlled handoff from the manual npx connector; verify device returns online with only the permanent service."
  - "After Astra RDC service is proven, remove stale Astra ~/.npm/_npx cache and any duplicate old autostart entry if present."
  - "Reboot via RED OS unified GRUB and test the explicit Windows 11 entry."
  - "On final Windows pass, verify Windows RDC Startup persistence, fully decrypt C: with manage-bde -off for a clean no-BitLocker demo-stand state, and verify decryption completes."
  - "Return to RED OS once more if needed to confirm its default/current-kernel path after cross-OS tests."
last_verified_commit: "ef4c4f1e91d14ff9fa6dc1c61c609a72860d33ed"
test_results:
  - "RED OS Boot0006 post-repair reboot: PASS."
  - "RED OS current kernel: PASS, 6.12.101-1.red80.x86_64."
  - "RED OS UEFI/mounts: PASS, p7=/, p6=/boot/efi, BootCurrent=0006."
  - "RED OS systemd-managed RDC sole-connector test: PASS."
  - "EFI-stub package-script durability check: PASS."
  - "Unified grub.cfg generation: PASS; grub2-script-check returned success."
  - "Explicit Astra chainloader runtime: PASS."
  - "Astra automatic GUI start: PASS."
  - "Astra failed systemd units: PASS, zero failed units."
  - "Astra RDC manual connectivity: PASS; permanent autostart handoff STAGED."
  - "Explicit Windows chainloader runtime: RETEST NEEDED."
  - "BitLocker restore requirement: REMOVED by user for ARVECTUM-DEMO demo stand."
resume_instruction: "Current OS is Astra Linux and arvectum-astra is online through a manually-started npx RDC connector. The RED OS GRUB explicit Astra entry and Astra automatic GUI startup both passed. Permanent Desktop Commander 0.2.50 has been copied to ~/.local/share/desktop-commander; wrapper ~/.local/bin/desktop-commander-remote and service ~/.config/systemd/user/desktop-commander-remote.service are staged. Use XDG_RUNTIME_DIR=/run/user/1001 and DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/1001/bus for user systemd commands. Enable the service, then perform a controlled handoff that terminates the current ~/.npm/_npx connector before restarting the service so the service is the sole presence owner. Verify list_devices/ping and only then delete ~/.npm/_npx. Next reboot through RED OS GRUB and test Windows 11. Do not re-enable BitLocker on ARVECTUM-DEMO."
execution:
  claimed_by: "ChatGPT"
  execution_id: "2026-09-15-redos-install"
  branch: "agent/redos-prep"
  manual_override: true
