version: 2
repository: arvectum2/proxy-launcher
task_id: APL-MAC-CHATGPT-ROUTING-20260919
status: in_progress
goal: "Diagnose and fix macOS APL routing so enabling APL does not break ChatGPT while coexisting safely with the user's existing launchd/PAC proxy."
authority: HUMAN
definition_of_done:
  - "Root cause reproduced or demonstrated with concrete runtime evidence."
  - "APL macOS routing/system-proxy logic is fixed without mutating the user's foreign launchd proxy state."
  - "Regression tests cover the failure mode, including multiple upstreams/failover and macOS proxy ownership where applicable."
  - "Relevant focused and full test suites pass."
  - "If product bytes change, prepare a new macOS test candidate without mutating published v0.2.10 or v0.2.10-macos-test.2 assets; publication remains outside autonomous authority."
  - "Checkpoint is completed with final commit/test evidence."
completed:
  - "Observed user report: ChatGPT fails only while APL is enabled and works again after disabling APL/restarting browser."
  - "Verified MacBook baseline while APL off: launchd com.user.tunnel-proxy owns 127.0.0.1:8080/1080/8082; Wi-Fi also has manual HTTP/HTTPS 68.209.60.141:8000 plus PAC http://127.0.0.1:8082/proxy.pac."
  - "Verified APL settings use 18080/11080/18082 and contain two upstreams: stale test 127.0.0.1:8888 first, real 68.209.60.141:8000 second."
  - "Verified APL start events at 20:35 local raised its listeners without a logged startup error."
  - "Inspected local_proxy_transport.py: HTTP CONNECT and SOCKS select an upstream after TCP connect/send, before validating upstream HTTP status; non-2xx proxy responses therefore do not fail over."
  - "Confirmed stale 127.0.0.1:8888 is currently connection-refused, so this defect alone does not explain the reported run because TCP failover reaches the second upstream."
  - "Controlled APL ON reproduction: APL listeners 18080/11080/18082 came up while foreign launchd 8080/1080/8082 stayed intact; macOS PAC changed to 18082 and manual HTTP/HTTPS proxy stayed intact."
  - "Connectivity through APL local HTTP proxy to chatgpt.com and api.openai.com succeeded at tunnel level; the real upstream is reachable."
  - "System URLSession fetched https://chatgpt.com with HTTP 200 and about 499 KB both APL OFF and APL ON; current failure is not reproduced in CFNetwork URLSession."
  - "Fresh-profile headless Chrome produced the same page size APL ON and OFF; no differential failure was observed."
  - "After every controlled probe, ran APL --stop and verified baseline PAC 8082 restored, APL listeners gone, and macos_proxy_backup.json cleared."
  - "Verified exactly one enabled hourly Proxy Launcher Watchdog already exists; no duplicate was created."
current: "Resolve browser-specific/system-proxy selection behavior and the 20:35 double-start evidence before deciding the smallest product fix."
pending:
  - "Inspect effective CFNetwork/PAC proxy selection for chatgpt.com with APL OFF versus ON."
  - "Inspect 20:35 process/lifecycle evidence for the two proxy core starts and determine whether a GUI start race exists."
  - "Implement only evidenced fixes; at minimum harden CONNECT/SOCKS response-aware upstream failover with regression tests."
  - "Run focused and full test suites."
  - "If product bytes change, build and validate a local macOS test candidate; do not publish without separate authority."
last_verified_commit: "890297a9c2e2c4a6f7ae39698134a50ad4b7c12f"
test_results:
  - "Existing v0.2.10-macos-test.2 Doctor previously passed 11/11 with no warnings, so Doctor did not detect this functional routing failure."
  - "MacBook controlled APL local HTTP CONNECT to chatgpt.com: tunnel established and upstream Cloudflare response received."
  - "MacBook URLSession APL OFF: HTTP 200, 499305 bytes."
  - "MacBook URLSession APL ON: HTTP 200, 499307 bytes."
resume_instruction: "Compare effective CFNetwork/PAC selection OFF vs ON and inspect double-start lifecycle evidence; then implement response-aware upstream failover plus any separately evidenced macOS fix, with tests."
updated_at: "2026-09-19T21:02:00+03:00"
execution:
  claimed_by: "ChatGPT"
  execution_id: "APL-MAC-CHATGPT-ROUTING-20260919"
  branch: "fix/macos-chatgpt-proxy-routing-20260919"
  base_commit: "890297a9c2e2c4a6f7ae39698134a50ad4b7c12f"
  manual_override: true
