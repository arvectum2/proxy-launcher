version: 2
repository: arvectum2/proxy-launcher
task_id: APL-REDOS-002
status: in_progress
goal: "Install RED OS 8.0.3 as third OS beside Windows and Astra without modifying existing partitions p1-p5."
definition_of_done:
  - "RED OS installed only on p6/p7."
  - "Windows, Astra and RED OS all boot."
  - "BitLocker protection restored only after triple-boot verification."
completed:
  - "RED OS 8.0.3 installed on p6/p7; p1-p5 preserved."
  - "RED OS Boot0006 repaired and real reboot test PASS; BootCurrent=0006."
  - "RED OS is running kernel 6.12.101-1.red80.x86_64; previous 6.12.92 retained as rollback kernel."
  - "Windows and Astra both booted successfully after RED OS installation; Astra clean GUI reboot retest remains pending."
  - "Windows Proxy Launcher 0.2.5 installed, configured and working; obsolete Windows launcher/test artifacts cleaned."
  - "Windows RTC UTC interoperability fixed; BitLocker remains intentionally suspended until final triple-boot validation."
  - "RED OS permanent Desktop Commander package exists under ~/.local/share/desktop-commander and persisted device session is valid."
  - "Created and enabled ~/.config/systemd/user/desktop-commander-remote.service with Restart=always."
  - "User restarted the RDC user service as requested; arvectumredos returned online and ping PASS."
  - "Verified the live RDC connector is solely the systemd-managed permanent process (MainPID 3164 at verification); no manual npx connector remains."
  - "Removed obsolete KDE autostart desktop-commander-remote.desktop and stale ~/.npm/_npx cache; systemd service remains enabled+active and device remains reachable."
current: "RED OS RDC persistence is now proven and cleaned up. A root-only GRUB/BLS inspection was launched via pkexec (PID 3325) and is waiting for the local graphical authorization prompt; user action is required only to approve that prompt."
pending:
  - "User approves the currently waiting pkexec authorization prompt for root-only GRUB/BLS inspection."
  - "Inspect /boot/loader/entries, /boot/grub2/grub.cfg, grubenv, /etc/grub.d and current EFI stub."
  - "Make the successful RED OS EFI-stub repair durable across future GRUB updates."
  - "Configure unified RED OS GRUB entries for current RED OS, fallback/recovery, Astra and Windows with short timeout."
  - "Reboot Astra and verify fly-dm/Xorg reaches GUI automatically."
  - "Test RED OS, Astra and Windows from unified menu."
  - "Final Windows boot: verify Windows RDC Startup persistence, remove obsolete _npx cache if unused, then re-enable BitLocker and confirm recovery protector."
last_verified_commit: "60794fc247e707bf9e7c4d46da01b92491c226b9"
test_results:
  - "RED OS Boot0006 post-repair reboot: PASS."
  - "RED OS current kernel: PASS, 6.12.101-1.red80.x86_64."
  - "RED OS UEFI/mounts: PASS, p7=/, p6=/boot/efi, BootCurrent=0006."
  - "RED OS multiple boot choices diagnosis: PASS; they are current+fallback kernels, not multiple OS installs."
  - "RED OS systemd-managed RDC sole-connector test: PASS; device online and ping PASS after service restart."
  - "RED OS RDC duplicate-start cleanup: PASS; old KDE autostart removed and ~/.npm/_npx removed; service stays active/enabled."
  - "Windows Proxy Launcher 0.2.5 cleanup/runtime: PASS."
  - "BitLocker remains suspended pending final validation."
  - "Astra automatic GUI start: RETEST NEEDED."
resume_instruction: "Current OS is RED OS. RDC persistence is fixed: use ~/.config/systemd/user/desktop-commander-remote.service; do not recreate the KDE autostart or npx connector. A pkexec root inspection is currently waiting as PID 3325. Ask the user only to approve the RED OS graphical authorization prompt. Then read PID 3325 output, inspect EFI stub/BLS/grubenv/grub.cfg/grub.d, make the boot repair durable, add/test explicit Astra and Windows chainloader entries while keeping one fallback kernel, then reboot Astra for GUI retest and complete triple-boot/BitLocker validation."
execution:
  claimed_by: "ChatGPT"
  execution_id: "2026-09-15-redos-install"
  branch: "agent/redos-prep"
  manual_override: true
