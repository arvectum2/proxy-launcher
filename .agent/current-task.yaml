version: 2
repository: arvectum2/proxy-launcher
task_id: APL-MAC-CHATGPT-ROUTING-20260919
status: in_progress
goal: "Diagnose and harden macOS APL routing so enabling APL does not strand browser traffic while coexisting safely with the user's existing launchd/PAC proxy."
authority: HUMAN
definition_of_done:
  - "The reported routing failure is investigated with concrete runtime evidence; any non-reproducible historical portion is explicitly separated from reproduced product defects."
  - "Reproduced upstream failover defects are fixed without mutating the user's foreign launchd proxy state."
  - "Diagnostics no longer imply that a local Doctor PASS proves external proxy/target connectivity."
  - "Regression tests cover rejected-upstream failover for HTTP CONNECT and SOCKS plus macOS diagnostic UX."
  - "Relevant focused tests pass and full-suite delta is compared against the exact base commit."
  - "If product bytes change, prepare and locally validate a new macOS test candidate without mutating published v0.2.10 or v0.2.10-macos-test.2 assets; publication remains outside autonomous authority."
  - "Checkpoint is completed with final commit/test/evidence."
completed:
  - "Observed user report: ChatGPT failed while APL was enabled and worked again after disabling APL/restarting browser."
  - "Verified baseline: foreign com.user.tunnel-proxy owns 127.0.0.1:8080/1080/8082 and Wi-Fi PAC http://127.0.0.1:8082/proxy.pac; manual HTTP/HTTPS upstream is also configured."
  - "Verified APL uses separate 18080/11080/18082 listeners and preserves the foreign launchd process/listeners during controlled ON/OFF tests."
  - "Verified APL profile contains two upstream entries: a stale localhost test endpoint first and one real upstream second; the legacy launchd implementation has broader real-upstream redundancy."
  - "Verified the localhost test upstream is currently connection-refused; current TCP failover reaches the real APL upstream."
  - "Controlled APL ON: chatgpt.com tunnel through 127.0.0.1:18080 succeeded; system URLSession fetched ChatGPT HTTP 200 (~499 KB) both OFF and ON."
  - "CFNetwork PAC evaluation resolves chatgpt.com to 127.0.0.1:8080 when APL is OFF and 127.0.0.1:18080 when APL is ON; active network remains en0/Wi-Fi."
  - "Fresh-profile headless Chrome showed no ON/OFF differential; the exact historical browser failure is no longer reproducible after the event."
  - "After every controlled probe, APL --stop restored PAC 8082, removed APL listeners, and cleared macos_proxy_backup.json."
  - "Reproduced a deterministic transport defect: first upstream returning HTTP 407/403 was treated as selected instead of triggering failover; new regression tests failed on the base behavior."
  - "Implemented response-aware CONNECT tunnelling: only upstream HTTP 2xx is accepted; rejected/malformed/failed upstreams are closed and the next upstream is tried for HTTP CONNECT and SOCKS."
  - "Updated Doctor PASS wording so it states only local problems were not found and directs external/end-to-end validation to 'Проверка соединения'."
  - "Made connection-test system-proxy labels platform-neutral instead of Windows-only wording."
  - "Targeted transport/connection/macOS/backend suite: 92 passed + 2 subtests."
  - "Full branch suite under Python 3.12/Tk: 1047 passed, 3 failed, 93 subtests; the exact same 3 Windows App Control contract failures reproduce on untouched base commit 890297a and are therefore pre-existing baseline failures."
  - "Verified exactly one enabled hourly Proxy Launcher Watchdog already exists; no duplicate was created."
current: "Commit the reproduced failover/diagnostic fixes, then build and locally validate an unpublished macOS candidate."
pending:
  - "Commit code/test/checkpoint changes."
  - "Build an unpublished macOS candidate from the exact branch commit."
  - "Validate packaged Doctor/codesign/architecture and controlled runtime on MacBook without replacing published assets."
  - "Push branch/open review-ready PR if repository policy permits; do not publish a release."
  - "Complete checkpoint with final exact commit and evidence."
last_verified_commit: "890297a9c2e2c4a6f7ae39698134a50ad4b7c12f"
test_results:
  - "TDD before fix: tests/test_local_proxy_transport.py => 2 fail, 5 pass; failures prove 407/403 did not fail over."
  - "After fix: tests/test_local_proxy_transport.py => 7/7 pass."
  - "Focused transport/core/connection/network suite => 76/76 pass."
  - "Focused macOS/backend/transport/connection suite => 92 pass + 2 subtests."
  - "Full branch suite => 1047 pass / 3 fail / 93 subtests; all 3 failures are pre-existing Windows App Control baseline failures."
  - "Exact base 890297a targeted baseline => same 3 Windows App Control failures, 6 pass."
  - "git diff --check and compileall on changed Python files => PASS."
resume_instruction: "Commit the current failover + diagnostic UX implementation, then build an unpublished macOS candidate and validate it on Mac mini/MacBook while leaving the installed test2/baseline network state recoverable."
updated_at: "2026-09-19T21:20:00+03:00"
execution:
  claimed_by: "ChatGPT"
  execution_id: "APL-MAC-CHATGPT-ROUTING-20260919"
  branch: "fix/macos-chatgpt-proxy-routing-20260919"
  base_commit: "890297a9c2e2c4a6f7ae39698134a50ad4b7c12f"
  manual_override: true
