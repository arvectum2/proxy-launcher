version: 2
repository: arvectum2/proxy-launcher
task_id: APL-MACOS-SLEEP-ROUTE-REGRESSION-20260920
status: in_progress
goal: "Prevent stale APL relay tunnels from surviving macOS sleep/wake and breaking ChatGPT until route/browser state changes."
definition_of_done:
  - "Root cause supported by live MacBook wake/process/proxy evidence."
  - "Relay detects suspend gaps and retires pre-sleep tunnels promptly after wake."
  - "Existing exact macOS proxy rollback semantics remain unchanged."
  - "Focused transport/network/macOS tests and relevant full suite pass."
  - "Fix is committed and pushed; review/CI state is recorded."
completed:
  - "Confirmed real MacBook wake at 2026-09-20 22:10:27 MSK."
  - "Confirmed installed candidate c0ea60a is functionally equivalent to current transport/backend code for this failure class."
  - "Confirmed upstream CONNECT returned 200 immediately after wake and in controlled post-wake APL tests; PAC/upstream were not dead."
  - "Confirmed APL ON uses exclusive PAC 18082 with manual HTTP/HTTPS disabled; OFF restores the pre-existing legacy route exactly."
  - "Confirmed legacy com.user.tunnel-proxy remains installed with KeepAlive/WatchPaths but does not mutate system proxy settings."
  - "Root cause evidence: a relay after multi-hour sleep reported elapsed_ms=60374 because time.monotonic() on macOS excludes system sleep."
  - "Implemented relay suspend-gap detection using wall-clock minus monotonic deltas; pre-sleep relay closes with resume_after_sleep before forwarding post-wake bytes."
  - "Preserved the existing 300-second awake-idle relay timeout and system-proxy ownership/rollback behavior."
  - "Focused tests passed: 38 tests plus 2 subtests under pytest; final focused unittest rerun 38/38 OK."
  - "Full suite passed with Python 3.12/Tk: 898 tests OK."
  - "Built frozen macOS 0.2.11 candidate with PyInstaller 6.22.0; codesign --verify --deep --strict passed."
  - "Live MacBook candidate validation passed: exclusive PAC 18082, listeners 18080/18082/11080, ChatGPT TCP/TLS and CFNetwork route succeeded; candidate then stopped and exact prior OFF proxy state was restored."
  - "Re-enabled the existing single hourly Proxy Launcher Watchdog; no duplicate automation was created."
current: "Commit and push the tested fix, open a PR, inspect exact-head CI and repository merge authority."
pending:
  - "Commit code/tests and checkpoint, push branch."
  - "Open/update PR with root-cause and validation evidence."
  - "Inspect exact-head required CI and repository authority; merge only if explicitly permitted."
last_verified_commit: "351a0350e287cc5746d6cc69bf3268f316120b5c"
test_results:
  - "pytest focused: 38 passed, 2 subtests passed."
  - "unittest focused: 38/38 OK."
  - "unittest full suite: 898/898 OK."
  - "macOS frozen candidate 0.2.11 build and codesign verification: PASS."
  - "real MacBook post-wake candidate route validation: PASS; prior OFF state restored."
resume_instruction: "Commit the tested relay suspend-gap fix and tests on fix/macos-sleep-route-regression-20260920, push, open PR, then inspect exact-head CI and merge authority. Do not repeat live MacBook diagnosis or candidate validation unless code changes."
updated_at: "2026-09-20T22:38:00+03:00"
execution:
  claimed_by: "ChatGPT"
  execution_id: "APL-MACOS-SLEEP-ROUTE-REGRESSION-20260920"
  branch: "fix/macos-sleep-route-regression-20260920"
  base_commit: "351a0350e287cc5746d6cc69bf3268f316120b5c"
  manual_override: true
