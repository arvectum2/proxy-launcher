version: 2
repository: arvectum2/proxy-launcher
task_id: APL-REDOS-002
status: in_progress
goal: "Install RED OS 8.0.3 as third OS beside Windows and Astra without modifying existing partitions p1-p5."
definition_of_done:
  - "RED OS installed only on p6/p7."
  - "Windows, Astra and RED OS all boot from the unified menu."
  - "ARVECTUM-DEMO remains a low-complexity demo stand; BitLocker is not required and must not be re-enabled as part of this task."
completed:
  - "RED OS 8.0.3 installed on p6/p7; p1-p5 preserved."
  - "RED OS Boot0006 repaired and real reboot test PASS; BootCurrent=0006."
  - "RED OS is running kernel 6.12.101-1.red80.x86_64; previous 6.12.92 retained as rollback kernel plus rescue entry."
  - "Windows and Astra both booted successfully after RED OS installation."
  - "Windows Proxy Launcher 0.2.5 installed, configured and working; obsolete Windows launcher/test artifacts cleaned."
  - "Windows RTC UTC interoperability fixed; BitLocker protection is currently suspended."
  - "RED OS Desktop Commander persistence fixed with ~/.config/systemd/user/desktop-commander-remote.service; sole-connector/ping test PASS."
  - "Removed obsolete RED OS KDE RDC autostart and stale ~/.npm/_npx cache."
  - "Root GRUB/BLS inspection completed. BLS contains current 6.12.101, fallback 6.12.92 and rescue; grubby default-kernel is 6.12.101 and default-index is 0."
  - "Confirmed repaired /boot/efi/EFI/redos/grub.cfg is owned by grub2-efi-x64 and grub2-common scriptlet preserves an already-unified EFI stub."
  - "Backed up /etc/default/grub, /etc/grub.d/40_custom and /boot/grub2/grub.cfg with timestamp 20260916-160707."
  - "Installed explicit chainloader entries for Windows 11 and Astra Linux 1.8 using shared ESP UUID 3EE2-B918; verified both EFI targets exist before regeneration."
  - "Set GRUB_TIMEOUT_STYLE=menu and GRUB_DISABLE_OS_PROBER=true; kept BLS enabled and retained rollback/rescue kernels."
  - "Regenerated /boot/grub2/grub.cfg atomically; grub2-script-check PASS; EFI stub remained unchanged."
  - "User clarified ARVECTUM-DEMO is the disposable/low-data three-OS demo stand, distinct from the Windows development laptop whose user is GN. Do not restore BitLocker on this stand."
  - "Explicit `Astra Linux 1.8` entry from RED OS GRUB runtime test: PASS. Astra booted while UEFI BootCurrent remained 0006 as expected for chainloading from RED OS."
  - "Astra clean GUI boot retest: PASS. fly-dm is enabled+active; Xorg :0 vt7 and fly-wm started automatically; zero failed systemd units."
  - "Astra Desktop Commander 0.2.50 migrated to permanent ~/.local/share/desktop-commander and enabled user systemd autostart; sole-connector/ping PASS; stale ~/.npm/_npx removed."
  - "Explicit `Windows 11` entry from RED OS GRUB runtime test: PASS; Windows booted normally."
  - "Windows RDC Startup entry `Arvectum Desktop Commander.cmd` is present and did launch the permanent `%LOCALAPPDATA%\\Programs\\DesktopCommander` copy automatically after logon. A separate manual npx RDC tree was also present, creating a duplicate same-device session."
  - "Windows Proxy Launcher runtime verified after user recovery/restart: v0.2.5 core listens on 127.0.0.1 ports 1080/8080/8082 and enables PAC at http://127.0.0.1:8082/proxy.pac."
  - "Added explicit Windows Startup entry `Arvectum Proxy Launcher.cmd` with a 12-second logon delay and canonical `--start`. Live idempotency test PASS: when core was already running, 0.2.5 logged `proxy_core.already.running.enabling.system.proxy` and kept the same listener process."
current: "Windows boot validation exposed a duplicate RDC presence issue: the permanent Startup-managed RDC was the connector servicing tool calls, while a manually launched npx RDC duplicate also existed. Force-terminating the duplicate inner server caused the shared device record to be marked offline (same broker presence behavior previously seen on Linux), even though the permanent copy was running. One local restart of the permanent Windows RDC is required to reassert online state; then finish duplicate cleanup, BitLocker removal, and final reboot tests."
pending:
  - "User performs one local restart of the permanent Windows RDC (not npx) so ARVECTUM-DEMO returns online."
  - "After reconnect, verify only permanent `%LOCALAPPDATA%\\Programs\\DesktopCommander` RDC processes remain; remove stale npm-cache/_npx only when unused."
  - "Verify the new Proxy Launcher Startup entry is present and use the next reboot as the real autostart/network recovery test."
  - "Fully decrypt Windows C: with manage-bde -off for a clean no-BitLocker demo-stand state; verify decryption completes."
  - "Perform final reboot cycle: RED OS default/current-kernel path PASS, then Windows PASS with automatic RDC + Proxy Launcher/network; mark task completed when all DoD items pass."
last_verified_commit: "42c4f31f2bdb22d6f6cb185a9ea96f8d8dc2f8ba"
test_results:
  - "RED OS Boot0006 post-repair reboot: PASS."
  - "RED OS current kernel: PASS, 6.12.101-1.red80.x86_64."
  - "RED OS UEFI/mounts: PASS, p7=/, p6=/boot/efi, BootCurrent=0006."
  - "RED OS systemd-managed RDC sole-connector test: PASS."
  - "EFI-stub package-script durability check: PASS."
  - "Unified grub.cfg generation: PASS; grub2-script-check returned success."
  - "Explicit Astra chainloader runtime: PASS."
  - "Astra automatic GUI start: PASS; zero failed units."
  - "Astra RDC permanent user-systemd autostart: PASS."
  - "Explicit Windows chainloader runtime: PASS."
  - "Windows permanent RDC Startup launch: PASS, but duplicate manual npx cleanup temporarily marked broker device offline; reconnect needed."
  - "Windows Proxy Launcher 0.2.5 current runtime/listeners: PASS."
  - "Windows explicit Proxy Launcher Startup script creation + live idempotency test: PASS; reboot test pending."
  - "BitLocker restore requirement: REMOVED by user for ARVECTUM-DEMO demo stand."
resume_instruction: "Current OS is Windows on ARVECTUM-DEMO. The machine booted successfully via the explicit RED OS GRUB `Windows 11` chainloader. Permanent RDC Startup did work, but a manual npx RDC duplicate was also running. Killing the duplicate inner server made the broker mark the shared device offline even though the permanent copy had been servicing tool calls. Have the user locally stop any remaining Desktop Commander node processes and launch only `%APPDATA%\\Microsoft\\Windows\\Start Menu\\Programs\\Startup\\Arvectum Desktop Commander.cmd`; do not use npx. Once ARVECTUM-DEMO is online, verify permanent sole connector, remove stale npm-cache/_npx, verify Proxy Launcher Startup script, fully decrypt C: (do not re-enable BitLocker), then final reboot-test RED OS and Windows automatic RDC/Proxy Launcher/network and mark completed."
execution:
  claimed_by: "ChatGPT"
  execution_id: "2026-09-15-redos-install"
  branch: "agent/redos-prep"
  manual_override: true
