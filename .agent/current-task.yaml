version: 2
repository: arvectum2/proxy-launcher
task_id: APL-MACOS-SLEEP-WAKE-LIVE-RECOVERY-20260922
status: in_progress
goal: "Make macOS APL remain reliably usable during normal operation and across network/sleep transitions without requiring APL disable or full browser restart."
authority: OPERATOR
definition_of_done:
  - "No unintended GUI --stop/rollback and no false recovery-required state during normal lifecycle transitions."
  - "Stored APL settings and mandatory connectivity bypasses remain valid."
  - "Safari/Chrome traffic remains usable through APL during ordinary runtime and after sleep/network transitions."
  - "A narrow browser-network repair path can be invoked without stopping APL, changing system proxy state, closing browser main processes or tabs."
  - "Focused/full/static/build/CI checks show no new regressions."
  - "Physical user acceptance confirms stable browsing through APL."
completed:
  - "Strict macOS worker ownership recovery, PAC-health false STOPPED fix, wake/HID guard, synchronous destructive-handler guard, and deferred Off confirmation implemented."
  - "Latest user FAIL around 21:57 MSK occurred with no full pmset Sleep/Wake; browser resume recovery correctly did not fire. Remaining issue is therefore not sleep-specific."
  - "Safari PID 41504 was the affected foreground browser immediately before recovery; Safari was closed at 21:57:01 and APL became frontmost."
  - "APL worker PID 42930 remained transport-healthy before recovery: 31 fresh CONNECT 200 responses, 0 upstream/relay errors, and a final relay delivered 223194 bytes upstream-to-client seconds before manual Off."
  - "Manual Off was explicit and correct: confirmation_requested -> confirmed -> --stop."
  - "Found false GUI recovery-state race: --stop is asynchronous; FocusIn can call refresh_status after worker death but before rollback backup deletion, briefly rendering 'Восстановить настройки сети' although normal rollback is still completing."
  - "Implemented lifecycle-action pending state for start/stop/rollback; FocusIn reconciliation is suppressed while lifecycle mutation is pending and terminal callbacks clear the flag only after final state."
  - "Added macOS 'Восстановить связь браузера' action: recycles only proven Safari/Chrome network services, never spawns APL lifecycle commands or mutates proxy settings."
  - "Found historical acceptance contamination in live no_proxy.txt: final line was literal 'gsp1.apple.comapl-mac-008.invalid'. The acceptance-only apl-mac-008.invalid came from APL-MAC-008 test documentation; file mtime dated 2026-08-17."
  - "Backed up malformed live no_proxy as no_proxy.txt.bak-20260922-2157 and restored exact canonical repo no_proxy file while APL was OFF."
  - "Promoted captive.apple.com and gsp1.apple.com into mandatory DEFAULT_NO_PROXY so corruption/removal from the user file cannot silently proxy these connectivity endpoints."
  - "Safari/network logs immediately before FAIL show active mask.icloud.com / Apple Private Relay related traffic over the system proxy path; Apple documents potential incompatibility between Private Relay and third-party VPN/filtering/network settings, but no explicit Private Relay failure has yet been proven."
  - "No automatic mask.icloud.com bypass was added because that would route Safari privacy traffic outside APL and change product semantics."
  - "181 focused macOS/GUI/routing/runtime tests PASS."
  - "Full suite: 1116 passed plus 3 historical unrelated Windows App Control failures."
  - "Changed-file Ruff F,E9,I PASS; git diff --check PASS."
current: "Commit lifecycle UI race fix + mandatory Apple connectivity bypass + manual browser repair action. Build/install exact macOS candidate and enable APL for a controlled physical test."
pending:
  - "Commit code/tests/checkpoint."
  - "Governed build/license/codesign and exact install."
  - "Enable APL and verify worker/listeners/system route/curl."
  - "Push/update PR #155 and inspect exact-head CI."
  - "Controlled user test: if Safari becomes unusable while APL remains ON, click 'Восстановить связь браузера' first, without Off or browser relaunch, and report whether traffic immediately returns."
  - "If repair succeeds, isolate browser-session trigger and productize guarded recovery. If repair fails, abandon browser-session hypothesis and capture target/system network evidence before any Off."
last_verified_commit: "7aceea41aefccb4c62bd0fb202b356b7aa49a3b7"
test_results:
  - "181 focused tests PASS."
  - "Full suite: 1116 passed, 3 historical unrelated Windows App Control failures."
  - "Changed-file Ruff F,E9,I PASS; git diff --check PASS."
  - "Live user no_proxy repaired from canonical source while APL OFF."
resume_instruction: "Do not assume the remaining failure requires sleep. Build/install the current candidate. On the next Safari failure keep APL ON and use the new 'Восстановить связь браузера' action before any Off/browser relaunch. Capture whether it restores traffic plus helper logs/PID changes. Do not automatically bypass Private Relay endpoints."
updated_at: "2026-09-22T22:12:00+03:00"
execution:
  claimed_by: "chatgpt"
  execution_id: "interactive-20260922-macos-sleep-wake-live-recovery"
  branch: "agent/apl-macos-net-recovery-20260921"
  base_commit: "a148e20bb82bd41dc87d47d14afea8027db6c3cb"
  lease_until: null
  manual_override: true
