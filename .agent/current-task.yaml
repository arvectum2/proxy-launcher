version: 2
repository: arvectum2/proxy-launcher
task_id: APL-REDOS-002
status: in_progress
goal: "Install RED OS 8.0.3 as third OS beside Windows and Astra without modifying existing partitions p1-p5."
definition_of_done:
  - "RED OS installed only on p6/p7."
  - "Windows, Astra and RED OS all boot."
  - "BitLocker protection restored only after triple-boot verification."
completed:
  - "RED OS 8.0.3 installed on p6/p7; p1-p5 preserved."
  - "RED OS Boot0006 repaired and real reboot test PASS; BootCurrent=0006."
  - "RED OS is running kernel 6.12.101-1.red80.x86_64; previous 6.12.92 retained as rollback kernel."
  - "Windows and Astra both booted successfully after RED OS installation; Astra clean GUI reboot retest remains pending."
  - "Windows Proxy Launcher 0.2.5 installed, configured and working; obsolete Windows launcher/test artifacts cleaned."
  - "Windows RTC UTC interoperability fixed; BitLocker remains intentionally suspended until final triple-boot validation."
  - "RED OS RDC permanent package exists under ~/.local/share/desktop-commander and persisted device session is valid."
  - "Confirmed the blank terminal output from ~/.local/bin/desktop-commander-remote is expected because the wrapper redirects stdout/stderr to ~/.local/state/desktop-commander-remote.log."
  - "Updated RED OS RDC wrapper to use a per-user flock singleton and clear proxy env only for the RDC process."
  - "Created and enabled user systemd service ~/.config/systemd/user/desktop-commander-remote.service with Restart=always; service started successfully and spawned the permanent RDC copy."
current: "RDC persistence migration is staged but needs one reconnect. A manually-started foreground permanent RDC process and the new systemd service were briefly running together. Killing the foreground process caused the broker to mark the shared device offline even though the service process remained, revealing Desktop Commander presence semantics with duplicate same-device sessions. The user must restart the user systemd service once so it becomes the sole connection owner."
pending:
  - "User runs `systemctl --user restart desktop-commander-remote.service`; verify arvectumredos returns online and ping passes with only the systemd-managed permanent process."
  - "Remove/disable the old KDE .desktop RDC autostart after the systemd service is proven as sole connector; remove obsolete ~/.npm/_npx cache only after no process uses it."
  - "Obtain one root authorization and inspect /boot/loader/entries, /boot/grub2/grub.cfg, grubenv and /etc/grub.d."
  - "Make the successful RED OS EFI-stub repair durable across future GRUB updates."
  - "Configure unified RED OS GRUB entries for current RED OS, fallback/recovery, Astra and Windows with short timeout."
  - "Reboot Astra and verify fly-dm/Xorg reaches GUI automatically."
  - "Test RED OS, Astra and Windows from unified menu."
  - "Final Windows boot: verify Windows RDC Startup persistence, remove obsolete _npx cache if unused, then re-enable BitLocker and confirm recovery protector."
last_verified_commit: "749016befdfb756ee9cff36f13374651222a0f4c"
test_results:
  - "RED OS Boot0006 post-repair reboot: PASS."
  - "RED OS current kernel: PASS, 6.12.101-1.red80.x86_64."
  - "RED OS UEFI/mounts: PASS, p7=/, p6=/boot/efi, BootCurrent=0006."
  - "RED OS multiple boot choices diagnosis: PASS; they are current+fallback kernels, not multiple OS installs."
  - "Permanent RDC command manual launch: PASS; persisted session restored and device became online."
  - "User systemd RDC service creation/enable/start: PASS; service active with permanent copy."
  - "Duplicate-session handoff test: FAIL/expected limitation; terminating the foreground duplicate marked device offline although service still existed. Needs service restart as sole owner."
  - "Windows Proxy Launcher 0.2.5 cleanup/runtime: PASS."
  - "BitLocker remains suspended pending final validation."
  - "Astra automatic GUI start: RETEST NEEDED."
resume_instruction: "Current OS is RED OS. The user's terminal may be back at a prompt after the foreground RDC process was terminated during persistence testing. Ask the user to run `systemctl --user restart desktop-commander-remote.service` (no sudo). Then immediately check list_devices/ping for arvectumredos. If online, verify pgrep/systemctl shows only the systemd-managed permanent RDC process, remove ~/.config/autostart/desktop-commander-remote.desktop to avoid duplicate startup, and remove stale ~/.npm/_npx only after confirming no process references it. Then obtain root authorization via pkexec for GRUB/BLS inspection, make the EFI fix durable, add/test Astra and Windows entries, retest Astra GUI, and only after all three boot paths pass restore BitLocker on final Windows boot."
execution:
  claimed_by: "ChatGPT"
  execution_id: "2026-09-15-redos-install"
  branch: "agent/redos-prep"
  manual_override: true
