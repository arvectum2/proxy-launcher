version: 2
repository: arvectum2/proxy-launcher
task_id: APL-REDOS-002
status: in_progress
goal: "Install RED OS 8.0.3 as third OS beside Windows and Astra without modifying existing partitions p1-p5."
definition_of_done:
  - "RED OS installed only on p6/p7."
  - "Windows, Astra and RED OS all boot from the unified menu."
  - "ARVECTUM-DEMO remains a low-complexity demo stand; BitLocker is not required and must not be re-enabled as part of this task."
completed:
  - "RED OS 8.0.3 installed on p6/p7; p1-p5 preserved."
  - "RED OS Boot0006 repaired and real reboot test PASS; BootCurrent=0006."
  - "RED OS is running kernel 6.12.101-1.red80.x86_64; previous 6.12.92 retained as rollback kernel plus rescue entry."
  - "Windows and Astra both booted successfully after RED OS installation."
  - "Windows Proxy Launcher 0.2.5 installed, configured and working; obsolete Windows launcher/test artifacts cleaned."
  - "Windows RTC UTC interoperability fixed."
  - "RED OS Desktop Commander persistence configured with ~/.config/systemd/user/desktop-commander-remote.service and permanent ~/.local/share/desktop-commander copy."
  - "Removed obsolete RED OS KDE RDC autostart and stale ~/.npm/_npx cache."
  - "Root GRUB/BLS inspection completed. BLS contains current 6.12.101, fallback 6.12.92 and rescue; grubby default-kernel is 6.12.101 and default-index is 0."
  - "Confirmed repaired /boot/efi/EFI/redos/grub.cfg is owned by grub2-efi-x64 and grub2-common scriptlet preserves an already-unified EFI stub."
  - "Backed up /etc/default/grub, /etc/grub.d/40_custom and /boot/grub2/grub.cfg with timestamp 20260916-160707."
  - "Installed explicit chainloader entries for Windows 11 and Astra Linux 1.8 using shared ESP UUID 3EE2-B918; verified both EFI targets exist before regeneration."
  - "Set GRUB_TIMEOUT_STYLE=menu and GRUB_DISABLE_OS_PROBER=true; kept BLS enabled and retained rollback/rescue kernels."
  - "Regenerated /boot/grub2/grub.cfg atomically; grub2-script-check PASS; EFI stub remained unchanged."
  - "User clarified ARVECTUM-DEMO is the disposable/low-data three-OS demo stand, distinct from the Windows development laptop whose user is GN. Do not restore BitLocker on this stand."
  - "Explicit `Astra Linux 1.8` entry from RED OS GRUB runtime test: PASS."
  - "Astra clean GUI boot retest: PASS. fly-dm is enabled+active; Xorg :0 vt7 and fly-wm started automatically; zero failed systemd units."
  - "Astra Desktop Commander 0.2.50 migrated to permanent ~/.local/share/desktop-commander and enabled user systemd autostart; sole-connector/ping PASS; stale ~/.npm/_npx removed."
  - "Explicit `Windows 11` entry from RED OS GRUB runtime test: PASS; Windows booted normally."
  - "Windows RDC Startup entry `Arvectum Desktop Commander.cmd` launches the permanent `%LOCALAPPDATA%\\Programs\\DesktopCommander` copy automatically after logon."
  - "Windows RDC duplicate manual npx tree removed; stale npm-cache/_npx removed; current connector uses only the permanent copy and is online when Windows is running."
  - "Windows Proxy Launcher runtime verified: v0.2.5 core listens on 127.0.0.1 ports 1080/8080/8082 and enables PAC at http://127.0.0.1:8082/proxy.pac."
  - "Added Windows Startup entry `Arvectum Proxy Launcher.cmd` with a 12-second logon delay and canonical `--start`; live idempotency test PASS."
  - "Local elevated `manage-bde -off C:` was run by the user. BitLocker event log Event ID 770 at 2026-09-16 20:45:33 confirms decryption started for C:."
  - "Final default RED OS boots reach desktop on kernel 6.12.101 with BootCurrent=0006 and zero failed system units."
  - "Diagnosed RED OS RDC cold-boot failure as startup-before-network combined with a three-hour RTC transition. RED OS is configured for UTC RTC and the RDC unit now gates on `/usr/bin/nm-online -q --timeout=60`, RestartSec=10."
  - "RED OS automatic RDC cold-reboot proof after hardening: PASS. Without any manual RDC command, `arvectumredos` came online and answered ping; the user service is enabled+active, NRestarts=0, and started at the same second Wi-Fi reached CONNECTED_GLOBAL."
  - "On that proof boot, kernel 6.12.101, BootCurrent=0006, zero failed system units, and current NTP health all PASS. Chrony is synchronized with ~0.1 ms offset and RTC policy reports `RTC in local TZ: no`."
  - "The first boot after converting RTC policy still inherited a raw local-time hardware-clock value from the transition boot and chrony stepped it back by ~10799s once. After that correction the live RTC is UTC; no manual RDC intervention was required and the connector remained healthy."
current: "RED OS automatic RDC persistence is now proven across reboot. The remaining end-to-end task is the final clean Windows 11 boot: select Windows from the unified RED OS GRUB and do not manually repair networking or start either RDC or Proxy Launcher. Then verify both Windows autostarts, proxy/network functionality, and BitLocker full-decryption state."
pending:
  - "Reboot and select `Windows 11`; do not manually repair network or launch Proxy Launcher/RDC. Verify automatic RDC + Proxy Launcher/network after logon."
  - "Verify BitLocker reaches fully decrypted state."
  - "If Windows time is correct after the UTC-RTC transition, record cross-OS RTC interoperability as PASS."
  - "Mark task completed once clean Windows autostart/network and C: full decryption pass."
last_verified_commit: "f635c7a3dfda1b51951891855990ed2918eddfac"
test_results:
  - "RED OS Boot0006/default path: PASS."
  - "RED OS current kernel: PASS, 6.12.101-1.red80.x86_64."
  - "RED OS UEFI: PASS, BootCurrent=0006."
  - "RED OS failed system units: PASS, zero failed units."
  - "RED OS RDC automatic cold-reboot autostart after nm-online hardening: PASS; online without manual start, ping PASS, enabled+active, NRestarts=0."
  - "RED OS current time sync: PASS; chrony Leap Normal, System clock synchronized=yes, current offset approximately 0.0001s, RTC policy UTC."
  - "RED OS first post-conversion hardware RTC transition: one -10799.4s chrony step occurred; live RTC is now corrected UTC."
  - "Unified grub.cfg generation: PASS; grub2-script-check returned success."
  - "Explicit Astra chainloader runtime: PASS."
  - "Astra automatic GUI start: PASS; zero failed units."
  - "Astra RDC permanent user-systemd autostart: PASS."
  - "Explicit Windows chainloader runtime: PASS."
  - "Windows permanent RDC Startup/sole connector: PASS before final clean-cycle proof."
  - "Windows Proxy Launcher 0.2.5 current runtime/listeners: PASS before final clean-cycle proof."
  - "Windows explicit Proxy Launcher Startup script creation + live idempotency test: PASS; clean reboot test pending."
  - "BitLocker decryption start: PASS, event ID 770 confirms C: decryption began at 20:45:33."
resume_instruction: "Current OS is RED OS on ARVECTUM-DEMO. RED OS automatic RDC cold-reboot proof has passed after nm-online hardening: arvectumredos came online without manual intervention, service enabled+active, NRestarts=0, kernel 6.12.101, BootCurrent=0006, zero failed units. Current RTC policy is UTC and chrony is synchronized; the first transition boot still had one inherited -10799s correction. Next have the user reboot and select `Windows 11` from unified GRUB. They must not manually repair network or start RDC/Proxy Launcher. Once Windows is up, verify ARVECTUM-DEMO appears automatically, inspect Startup process creation times, proxy ports/PAC/network reachability, confirm Windows clock is correct, and verify BitLocker is fully decrypted. If all pass, mark task completed. Do not re-enable BitLocker."
execution:
  claimed_by: "ChatGPT"
  execution_id: "2026-09-15-redos-install"
  branch: "agent/redos-prep"
  manual_override: true
