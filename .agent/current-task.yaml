version: 2
repository: arvectum2/proxy-launcher
task_id: APL-MAC-DIAGNOSTICS-20260919
status: in_progress
goal: "Fix macOS diagnostics so Windows-only checks do not fail and listener conflicts are reported against the actual Arvectum local ports/process ownership."
authority: HUMAN
definition_of_done:
  - "platform.windows is not a FAIL on macOS; platform-specific diagnostics are skipped/not-applicable correctly."
  - "listeners.health on macOS checks the configured Arvectum local ports and distinguishes owned Launcher listeners from unrelated processes."
  - "Diagnostic UX gives actionable macOS-specific text without English-only Windows guidance."
  - "Regression tests cover macOS platform and listener scenarios on both macOS and cross-platform CI."
  - "Fresh macOS app build passes and installed test build reproduces correct diagnostics without changing network state."
  - "Fix is merged through CI and a new macOS test distribution is published if the user-facing binary changes."
completed:
  - "Observed user screenshot: FAIL platform.windows + WARN listeners.health on macOS 0.2.10."
  - "Preserved active iOS work separately; desktop diagnostics worktree created from current origin/main."
  - "Root cause confirmed: Doctor unconditionally evaluated platform.windows even on Darwin."
  - "Fixed supported platform evaluation: Windows/macOS/Linux each receive a platform-specific PASS; unknown desktop platforms fail."
  - "General engine/PAC diagnostics wording made platform-neutral."
  - "macOS listener probing now performs best-effort read-only lsof owner lookup and reports occupied port, process and PID."
  - "macOS listeners.health warning now gives Russian actionable guidance and recommends 18080/11080/18082 as an example."
  - "Proxy settings UI now exposes validated local HTTP/SOCKS5/PAC ports and a macOS one-click recommended-port preset."
  - "Doctor modal now displays each failing/warning check summary, not only the check id."
  - "Real Mac read-only validation: current 18080/11080/18082 configuration yields PASS 11/WARN 0/FAIL 0."
  - "Real Mac simulated legacy 8080/1080/8082 conflict yields PASS 10/WARN 1/FAIL 0 and correctly identifies Python PID 1139 on all three ports."
  - "Aqua Settings render probe passed at 719x657 with local-port controls and recommended-port preset."
  - "Targeted doctor/windows-diagnostics/proxy-gui/macOS visual tests passed 49/49."
  - "Full repository regression passed 879/879; git diff --check passed."
current: "Implementation is fully green locally. Commit/push exact fix, build/install exact macOS app, verify Doctor, then merge and publish test build 2."
pending:
  - "Build/install fresh macOS app safely and verify diagnostics."
  - "Merge via PR and publish replacement macOS test prerelease under a new test tag."
last_verified_commit: "952438b23c875b875dd39acfc3da384622e9e8d8"
test_results:
  - "Targeted doctor/windows-diagnostics/proxy-gui/macOS visual suite: 49/49 PASS."
  - "Real macOS Doctor current-port scenario: PASS 11/WARN 0/FAIL 0."
  - "Real macOS legacy-port conflict simulation: PASS 10/WARN 1/FAIL 0 with lsof owner attribution."
  - "Aqua Settings render probe: PASS."
  - "Full repository regression: 879/879 PASS."
resume_instruction: "Run full repository regression from fix/macos-diagnostics-20260919. If green, build/install a private macOS app without changing network state, rerun Doctor in the built app, merge via PR, and publish v0.2.10-macos-test.2 as a separate non-notarized test prerelease."
updated_at: "2026-09-19T17:36:00+03:00"
execution:
  claimed_by: "ChatGPT"
  execution_id: "APL-MAC-DIAGNOSTICS-20260919"
  branch: "fix/macos-diagnostics-20260919"
  base_commit: "952438b23c875b875dd39acfc3da384622e9e8d8"
  manual_override: true
