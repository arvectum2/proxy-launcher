version: 2
repository: arvectum2/proxy-launcher
task_id: APL-MACOS-SLEEP-WAKE-LIVE-RECOVERY-20260922
status: in_progress
goal: "Prevent macOS wake/HID delivery from causing an unintended destructive GUI stop/rollback against a healthy APL worker."
authority: OPERATOR
definition_of_done:
  - "A long clamshell wake preserves worker/listeners/system route/external connectivity."
  - "A queued HID/button event cannot outrun wake protection and spawn --stop/--rollback."
  - "A single accidental/synthetic macOS Off activation cannot stop the proxy without explicit confirmation."
  - "Explicit user OFF/rollback remains available after guard/confirmation."
  - "Focused/full/static/build/CI checks show no new regressions."
  - "PR #155 and canonical evidence are current."
completed:
  - "Earlier fixes preserved strict macOS worker ownership across PID loss and removed PAC-health false STOPPED classification."
  - "Instrumented long-clamshell reproduction proved worker survives sleep while GUI later emits --stop."
  - "2026-09-22 15:50:25→15:55:09 long sleep: worker PID 24328 survived; GUI PID 24377 emitted --stop at 15:55:21.789 with macOS slow HID response."
  - "First wake guard used a 1 s Tk heartbeat and 15 s post-gap destructive-action window; live event-loop fault injection armed it successfully."
  - "Second observed FAIL at 17:04:06: installed guard build hash af86c94c... was present, worker PID 29274 remained healthy through 17:03:59, then existing GUI PID 29323 emitted --stop; no wake_guard.armed event preceded it."
  - "That failure proves Tk/AppKit can deliver a queued destructive button event before the first scheduled root.after heartbeat callback."
  - "pmset did not record a new full Sleep/Wake immediately before the 17:04 failure, so protection cannot depend solely on a canonical sleep gap."
  - "Implemented synchronous guard arming inside every destructive handler: _block_destructive_wake_action compares current wall time with the last heartbeat before any --stop/--rollback can spawn."
  - "If the destructive handler itself observes a stale GUI gap >=5 s, it arms the 15 s guard with source=destructive_action and blocks the action."
  - "Added explicit macOS Off confirmation with default=no, so a single synthetic/accidental HID activation cannot directly stop the worker even when no long event-loop gap is detected."
  - "Rollback and settings-restart retain existing confirmation/guard paths."
  - "Focused suite PASS: 136 tests."
  - "Full suite: 1099 passed plus 3 historical unrelated Windows App Control failures."
  - "Changed-file Ruff F,E9,I PASS; git diff --check PASS."
current: "Commit synchronous self-arming guard + explicit macOS Off confirmation, build/install exact candidate, push PR #155, verify exact-head CI, then perform one final human long clamshell acceptance."
pending:
  - "Commit code/tests/checkpoint and build governed macOS app."
  - "Install exact build and verify worker/listeners/system route/external connectivity."
  - "Push/update PR #155 and inspect exact-head CI."
  - "Human long clamshell 2+ minutes on installed final build; immediately inspect wake_guard.armed/blocked and lifecycle.spawn before mutation."
  - "If PASS, mark task completed; if FAIL, exact log must now distinguish no-gap confirmation path vs synchronous guard path."
last_verified_commit: "037a05903c33f70ebd845f4606c0a5e8a5879602"
test_results:
  - "136 focused tests PASS."
  - "Full suite: 1099 passed, 3 historical unrelated Windows App Control failures."
  - "Changed-file Ruff F,E9,I PASS; git diff --check PASS."
  - "Previous installed guard hash af86c94c46bfb266753df06eb3729446b9d93a4f0f35fdbd9befa759fe269feb reproduced GUI --stop before timer-based guard arm."
resume_instruction: "Do not revisit prior root-cause paths. Build/install the current synchronous self-arming guard + default-no macOS Off confirmation. Next physical test must inspect logs before mutation. PASS requires no lifecycle --stop/--rollback from wake/HID and working worker/listeners/route/curl."
updated_at: "2026-09-22T17:10:00+03:00"
execution:
  claimed_by: "chatgpt"
  execution_id: "interactive-20260922-macos-sleep-wake-live-recovery"
  branch: "agent/apl-macos-net-recovery-20260921"
  base_commit: "a148e20bb82bd41dc87d47d14afea8027db6c3cb"
  lease_until: null
  manual_override: true
