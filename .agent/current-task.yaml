version: 2
repository: arvectum2/proxy-launcher
status: in_progress
task_id: APL-CI-REL-EVIDENCE-ELIGIBILITY-001
goal: Prevent false-red Release Evidence Package runs on main commits that do not trigger the required exact-SHA Linux Debian packaging workflow.
authority: HUMAN
definition_of_done:
- Automatic workflow_run evidence packaging detects whether the source SHA is release-evidence eligible before polling required gates.
- Non-release commits such as .agent-only checkpoint merges finish the Release Evidence workflow successfully without fabricating an evidence artifact.
- Manual workflow_dispatch remains fail-closed and attempts full exact-SHA evidence packaging.
- Actual release-relevant commits with Linux Debian CI continue to collect and seal the full evidence package.
- Contract tests and exact-head CI pass; fix is merged to main and current main no longer has a false-red evidence run.
completed:
- Implemented automatic Release Evidence eligibility guard: workflow_run checks for an exact-SHA Linux Debian push run before collecting release evidence; non-release commits exit successfully without fabricating an artifact; manual dispatch remains full/fail-closed.
- Hardened public release validation to require a live exact-SHA Release Evidence artifact, not merely a successful evidence workflow conclusion.
- Added regression coverage for eligibility skip and artifact requirement; focused release/repository suite is 28/28 PASS; workflow YAML parse and git diff --check PASS.
- Root cause confirmed: Release Evidence is triggered by every successful main SAST push, while linux-deb.yml is path-filtered; .agent-only main commit 6f034850... therefore had no exact-SHA Linux Debian run and the evidence collector would wait until timeout.
current: Commit implementation, open PR, obtain exact-head CI, merge, and verify current-main behavior.
pending:
- Open PR and obtain green exact-head CI.
- Merge to main and verify the new automatic evidence run skips cleanly when Linux Debian CI is absent.
- Mark task completed and preserve checkpoint history.
last_verified_commit: 6f034850841b999c7b7e63b4ae44c2147f0b292a
test_results:
- Root-cause inspection: linux-deb.yml is path-filtered; sast.yml runs on every main push; release-evidence.yml currently polls exact-SHA linux-deb for up to 10 minutes.
updated_at: '2026-09-16T22:03:00Z'
resume_instruction: Commit and push the eligibility/artifact-gate fix, open PR, wait for exact-head CI, merge, then verify current-main Release Evidence exits successfully without waiting for Linux Debian on a non-release commit; close checkpoint.
execution:
  claimed_by: chatgpt
  execution_id: APL-CI-REL-EVIDENCE-ELIGIBILITY-001-20260917
  branch: fix/release-evidence-eligibility
  base_commit: 6f034850841b999c7b7e63b4ae44c2147f0b292a
  lease_until: null
  manual_override: true
