version: 2
repository: arvectum2/proxy-launcher
status: in_progress
task_id: APL-LNX-011-FIREFOX-SYSTEM-PROXY
goal: "Reproduce and fix the real Astra/Fly Firefox failure when Firefox is set to Use system proxy settings while Proxy Launcher is active, without regressing NetworkManager rollback/recovery semantics."
authority: HUMAN
definition_of_done:
  - "Reproduce the user-reported Firefox system-proxy failure on the physical Astra host without disturbing the user's working profile."
  - "Identify the exact integration boundary between Firefox, Fly desktop proxy settings, NetworkManager PAC state, and the local Proxy Launcher/PAC service."
  - "If the defect is in Proxy Launcher, implement the smallest production-safe fix with regression coverage."
  - "Verify real browser/system-proxy end-to-end HTTPS routing through Proxy Launcher on Astra, including the user-reported ChatGPT path where feasible."
  - "Verify no regression in exact NetworkManager rollback/recovery and autostart behavior."
  - "Run relevant deterministic tests and required exact-head CI; merge fixes and mark the checkpoint completed only after evidence passes."
completed:
  - "User report captured: Firefox failed after switching from manual proxy to Use system proxy settings while Proxy Launcher was active."
  - "Current installed package is 0.2.5 amd64; Proxy Launcher local HTTP/SOCKS/PAC listeners are active on 127.0.0.1:8080/1080/8082."
  - "NetworkManager active Wi-Fi connection is proxy.method=auto with PAC URL http://127.0.0.1:8082/proxy.pac; rollback evidence exists."
  - "Direct curl through the local HTTP proxy succeeds (example.com HTTP 200), proving proxy-core/upstream transport is alive."
  - "Root cause proved on physical Astra/Fly: Firefox network.proxy.type=5 reads desktop GSettings, while 0.2.5 only published the PAC through NetworkManager."
  - "Installed local non-published 0.2.6-rc1 candidate on Astra; NetworkManager and org.gnome.system.proxy both switched to the Arvectum PAC."
  - "Isolated Firefox type=5 used Proxy Launcher egress IP 68.209.60.141 and opened the ChatGPT public loading path; live Firefox sockets connected to 127.0.0.1:8080."
  - "Candidate rollback returned NetworkManager and GSettings to exact baseline; NetworkManager comparator PASS, GSettings cmp PASS, pending rollback NO."
  - "User working Firefox profile remained manual (network.proxy.type=1) and XDG autostart remained enabled throughout validation."
current: "Publish the tested fix branch to GitHub, run exact-head CI, merge only after required checks pass, then close the checkpoint."
pending:
  - "Run exact-head deterministic tests and GitHub CI on the tested fix branch."
  - "Merge the fix after required checks pass and update checkpoint to completed."
last_verified_commit: "6c06db1397ded99d259a2a1412a0f5c20942cd44"
test_results:
  - "Current local Proxy Launcher HTTP proxy: HTTPS tunnel to example.com returned HTTP 200."
  - "Current NetworkManager PAC endpoint: HTTP 200."
  - "Isolated Firefox type=5 reproduced direct-IP bypass while NetworkManager PAC was active."
  - "Temporary GSettings auto/PAC test made isolated Firefox use the Proxy Launcher egress IP, proving the missing desktop proxy integration."
  - "Implemented GSettings desktop proxy snapshot/apply/restore with rollback schema v2 and v1 recovery compatibility; focused suite 79/79 PASS."
  - "Complete deterministic suite after the fix: 812/812 PASS."
  - "Built local non-published 0.2.6-rc1 .deb candidate SHA256 f4cb2b1c937e7686c64707439608f97a486f27ef3460defd6c894e2ad887b93c."
  - "Physical Firefox system-proxy E2E: PASS; Firefox type=5 egress matched local Proxy Launcher egress 68.209.60.141."
  - "Physical rollback: PASS; NetworkManager exact comparator PASS and GSettings baseline cmp PASS; pending rollback NO."
updated_at: "2026-09-15T04:48:02Z"
resume_instruction: "Push the tested APL-LNX-011 tree to fix/apl-lnx-011-firefox-system-proxy, run exact-head CI, merge only if required checks are green, then set status completed and record the merge commit. Do not repeat physical Astra E2E unless code changes after the tested commit."
execution:
  claimed_by: "human-owner"
  execution_id: "APL-LNX-011-ASTRA-FIREFOX-20260914"
  branch: "fix/apl-lnx-011-firefox-system-proxy"
  base_commit: "6c2ef0b83b2cce68c465556d94a60262d64d20e2"
  lease_until: null
  manual_override: true
