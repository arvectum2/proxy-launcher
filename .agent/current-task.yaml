version: 2
repository: arvectum2/proxy-launcher
status: in_progress
task_id: APL-LNX-011-FIREFOX-SYSTEM-PROXY
goal: "Reproduce and fix the real Astra/Fly Firefox failure when Firefox is set to Use system proxy settings while Proxy Launcher is active, without regressing NetworkManager rollback/recovery semantics."
authority: HUMAN
definition_of_done:
  - "Reproduce the user-reported Firefox system-proxy failure on the physical Astra host without disturbing the user's working profile."
  - "Identify the exact integration boundary between Firefox, Fly desktop proxy settings, NetworkManager PAC state, and the local Proxy Launcher/PAC service."
  - "If the defect is in Proxy Launcher, implement the smallest production-safe fix with regression coverage."
  - "Verify real browser/system-proxy end-to-end HTTPS routing through Proxy Launcher on Astra, including the user-reported ChatGPT path where feasible."
  - "Verify no regression in exact NetworkManager rollback/recovery and autostart behavior."
  - "Run relevant deterministic tests and required exact-head CI; merge fixes and mark the checkpoint completed only after evidence passes."
completed:
  - "User report captured: Firefox failed after switching from manual proxy to Use system proxy settings while Proxy Launcher was active."
  - "Current installed package is 0.2.5 amd64; Proxy Launcher local HTTP/SOCKS/PAC listeners are active on 127.0.0.1:8080/1080/8082."
  - "NetworkManager active Wi-Fi connection is proxy.method=auto with PAC URL http://127.0.0.1:8082/proxy.pac; rollback evidence exists."
  - "Direct curl through the local HTTP proxy succeeds (example.com HTTP 200), proving proxy-core/upstream transport is alive."
current: "Reproduce Firefox 'Use system proxy settings' in an isolated temporary profile and determine whether Firefox reads NetworkManager PAC or a separate desktop/GSettings proxy source on Astra/Fly."
pending:
  - "Capture isolated Firefox system-proxy behavior and desktop proxy source reads."
  - "Implement/test fix if Proxy Launcher does not publish the proxy where Firefox expects it."
  - "Run physical Astra browser E2E and rollback checks."
  - "Run tests/CI, merge, update checkpoint to completed."
last_verified_commit: "6c2ef0b83b2cce68c465556d94a60262d64d20e2"
test_results:
  - "Current local Proxy Launcher HTTP proxy: HTTPS tunnel to example.com returned HTTP 200."
  - "Current NetworkManager PAC endpoint: HTTP 200."
updated_at: "2026-09-14T20:45:00Z"
resume_instruction: "On the Astra host, leave the user's working Firefox profile untouched. Create an isolated Firefox profile with network.proxy.type=5 (Use system proxy settings), then observe whether it uses NetworkManager PAC or desktop/GSettings proxy state. If it bypasses NetworkManager because desktop proxy mode remains none, fix Linux backend integration to publish/restore the desktop proxy source safely and add regression tests."
execution:
  claimed_by: "human-owner"
  execution_id: "APL-LNX-011-ASTRA-FIREFOX-20260914"
  branch: "fix/apl-lnx-011-firefox-system-proxy"
  base_commit: "6c2ef0b83b2cce68c465556d94a60262d64d20e2"
  lease_until: null
  manual_override: true
