version: 2
repository: arvectum2/proxy-launcher
task_id: APL-MACOS-SLEEP-WAKE-LIVE-RECOVERY-20260922
status: in_progress
goal: "Make macOS APL reliable during ordinary runtime and transitions, including Safari on macOS 27, with exact rollback."
authority: OPERATOR
definition_of_done:
  - "macOS traffic routes through APL in Safari/Chrome/system clients without browser-process hacks."
  - "No concurrent legacy localhost proxy stack remains able to retain stale Safari sessions."
  - "Start refuses unsafe/conflicting localhost PAC baselines."
  - "Stop/rollback preflights exact saved localhost PAC backing before worker termination."
  - "HTTP/HTTPS/SOCKS/PAC and bypass state are snapshotted/restored exactly."
  - "Focused/full/static/build/CI checks show no new regressions."
  - "Physical user acceptance confirms stable browsing and clean Off rollback."
completed:
  - "Worker ownership/PID, PAC-health false STOPPED, wake/HID, deferred Off confirmation, lifecycle UI race and no_proxy corruption fixes retained."
  - "Limit IP Address Tracking A/B completed and rejected Private Relay as primary root cause."
  - "System CFNetwork resolver independently proves APL HTTP proxy config is valid: proxied target -> 127.0.0.1:18080, bypass target -> DIRECT."
  - "APL PAC independently validates through CFNetwork evaluator: proxied target -> 127.0.0.1:18080, bypass target -> DIRECT."
  - "Safari 27 manual HTTP/HTTPS and PAC paths were unreliable despite valid system resolution."
  - "Legacy com.user.tunnel-proxy coexistence was proven harmful: Safari retained old established 127.0.0.1:8080 sessions after APL switched system settings. Legacy LaunchAgent is currently booted out."
  - "Decisive A/B: macOS system SOCKS proxy 127.0.0.1:11080 works with Safari 27. Fresh Safari PID 83813 established TCP to APL 11080; APL relayed upstream successfully."
  - "APL SOCKS handler already applies canonical host_bypasses_proxy policy and supports IPv4/domain/IPv6 destinations."
  - "Implemented SOCKS-first macOS backend in working tree: PAC + manual HTTP/HTTPS disabled while active, SOCKS set to APL, bypass preserved."
  - "Extended ProxyBackendConfig with optional socks_proxy_url; runtime resolves local_socks_port."
  - "macOS rollback schema bumped to v2 and snapshots web/secure/SOCKS/PAC/bypass."
  - "Implemented generic localhost PAC conflict/start guard and disable_preflight. Stop/rollback now preflight before killing worker."
  - "Live preflight fault check against absent legacy 8082 returned False while worker PID 80922, route and all APL listeners remained unchanged."
  - "SOCKS-first focused backend/transport/runtime suite: 121 PASS."
  - "Full suite: 1115 passed plus 3 historical unrelated Windows App Control failures."
  - "Ruff F,E9,I and git diff --check PASS."
current: "Commit SOCKS-first candidate and run a reversible legacy-free baseline viability test before replacing the installed app."
pending:
  - "Commit code/tests/checkpoint without untracked macos/TransparentProxyExtension prototype."
  - "Re-bootstrap legacy LaunchAgent, stop currently installed old APL cleanly, verify exact working legacy baseline."
  - "Temporarily disable legacy PAC and legacy agent while leaving baseline manual upstream; test a real CFNetwork/URLSession request. Restore legacy immediately if baseline is not viable."
  - "If legacy-free baseline is viable, keep legacy agent disabled, install/build SOCKS-first candidate, start APL from clean schema-v2 baseline."
  - "Physical Safari/Chrome/system client test through SOCKS 11080, then clean Off rollback test."
  - "Push/update PR #155 and exact-head CI."
last_verified_commit: "952f705bc95918d84218407e23fb8f08c24bc32e"
test_results:
  - "121 SOCKS/backend/transport/runtime focused tests PASS."
  - "Full suite: 1115 passed, 3 historical unrelated Windows App Control failures."
  - "Ruff F,E9,I PASS; git diff --check PASS."
  - "Physical Safari SOCKS A/B PASS: Safari -> 127.0.0.1:11080 -> APL -> upstream."
resume_instruction: "Continue with reversible baseline migration. First re-bootstrap com.user.tunnel-proxy and verify 8080/1080/8082, then stop old installed APL cleanly. Test legacy-free baseline before permanently leaving the legacy agent off. Do not install schema-v2 build over the active schema-v1 session."
updated_at: "2026-09-23T08:25:00+03:00"
execution:
  claimed_by: "chatgpt"
  execution_id: "interactive-20260922-macos-sleep-wake-live-recovery"
  branch: "agent/apl-macos-net-recovery-20260921"
  base_commit: "a148e20bb82bd41dc87d47d14afea8027db6c3cb"
  lease_until: null
  manual_override: true
