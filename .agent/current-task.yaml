version: 2
repository: arvectum2/proxy-launcher
task_id: APL-MACOS-SLEEP-WAKE-LIVE-RECOVERY-20260922
status: in_progress
goal: "Prevent macOS GUI input sequencing from unintentionally stopping a healthy APL worker or corrupting active network state."
authority: OPERATOR
definition_of_done:
  - "APL settings file remains intact and active network state is not destroyed by a single stale/accidental macOS input event."
  - "A macOS Off request cannot spawn --stop from the same AppKit mouse event that opened confirmation."
  - "Queued wake/HID events cannot outrun synchronous destructive-action guard."
  - "Explicit intentional OFF remains available via a distinct delayed confirmation."
  - "Focused/full/static/build/CI checks show no new regressions."
  - "Final installed candidate survives real user wake/use without unintended --stop."
completed:
  - "Verified user-reported 'broken network settings': proxy_settings.json itself is intact and unchanged since 2026-09-20; failure affected active system network state, not stored APL upstream/port configuration."
  - "At 20:53:11 MSK GUI PID 32670 spawned --stop, worker stopped, PID/backup markers were removed, and macOS restored legacy baseline HTTP/HTTPS upstream + legacy PAC."
  - "Unified log proves a real trackpad button down/up at 20:53:11.537-11.538, followed by AppKit sendAction at 20:53:11.553/11.567 and proxy_gui.lifecycle.spawn --stop at 20:53:11.571."
  - "The ~18 ms action-to-stop interval proves the synchronous confirmation modal was accepted by the same input sequence before a human could see/respond to it; default=no was insufficient."
  - "Restored APL immediately after evidence capture: worker healthy, HTTP/SOCKS/PAC listeners owned, system HTTP/HTTPS 127.0.0.1:18080, PAC disabled, external proxy connectivity PASS."
  - "Implemented 500 ms deferred macOS Off confirmation so the originating AppKit mouseUp completes before any modal exists."
  - "Added _off_confirmation_pending to suppress duplicate queued Off activations."
  - "Added structured off confirmation requested/confirmed/cancelled events."
  - "Existing synchronous stale-heartbeat guard remains before initial Off and is rechecked before the delayed confirmation."
  - "Focused suite PASS: 138 tests."
  - "Full suite: 1101 passed plus 3 historical unrelated Windows App Control failures."
  - "Changed-file Ruff F,E9,I PASS; git diff --check PASS."
current: "Commit deferred-confirmation fix, build/install exact macOS candidate, push PR #155, inspect exact-head CI, then run one physical wake/use acceptance."
pending:
  - "Commit code/tests/checkpoint."
  - "Governed macOS build/license/codesign and exact install."
  - "Verify installed worker/listeners/route/connectivity."
  - "Push/update PR #155 and inspect exact-head CI."
  - "Physical wake/use acceptance; confirm no unintended --stop and no active network rollback."
last_verified_commit: "c34092d218c10f9b89ddd88bbb330f0923284649"
test_results:
  - "138 focused tests PASS."
  - "Full suite: 1101 passed, 3 historical unrelated Windows App Control failures."
  - "Changed-file Ruff F,E9,I PASS; git diff --check PASS."
resume_instruction: "Build/install the deferred-confirmation candidate. Do not alter proxy_settings.json; it is intact. After install, verify healthy APL state. The next physical acceptance should inspect off confirmation request/confirmed/cancelled and lifecycle.spawn before any mutation."
updated_at: "2026-09-22T20:59:00+03:00"
execution:
  claimed_by: "chatgpt"
  execution_id: "interactive-20260922-macos-sleep-wake-live-recovery"
  branch: "agent/apl-macos-net-recovery-20260921"
  base_commit: "a148e20bb82bd41dc87d47d14afea8027db6c3cb"
  lease_until: null
  manual_override: true
