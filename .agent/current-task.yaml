version: 2
repository: arvectum2/proxy-launcher
status: in_progress
task_id: APL-REL-017-V0.2.6
goal: Publish a clean stable v0.2.6 release containing Windows and Astra Linux artifacts on GitHub and an independently verified
  GitVerse mirror.
authority: HUMAN
definition_of_done:
- Bump the canonical product/runtime/package version to 0.2.6 and document the material Linux/Astra Firefox system-proxy fix.
- Extend the canonical GitHub release pipeline so v0.2.6 publishes Windows portable, Windows installer, Astra Linux amd64
  Debian package, and SHA256SUMS from one immutable tag.
- Require exact-main green Windows P0, Windows installer, Linux Debian package, and release-evidence gates before publication.
- Provide a generic GitVerse release mirror path that mirrors the GitHub v0.2.6 release, losslessly wraps unsupported GitVerse
  extensions, and verifies canonical payload SHA-256 after public download.
- Validate the final Linux package on the physical Astra Linux 1.8 x86-64 stand, including install/upgrade, launch, autostart,
  PAC/system-proxy browser path, and rollback/recovery behavior.
- Merge the release PR to main with all required CI green, create immutable tag v0.2.6, and verify clean public releases on
  GitHub and GitVerse with matching payload hashes.
- Record final main/tag commit, public asset hashes, release URLs/evidence, and mark this checkpoint completed.
completed:
- Recovered prior completed Windows system-proxy parity task and preserved it in .agent/checkpoints/APL-WIN-SYSTEM-PROXY-PARITY-001.yaml.
- 'Confirmed physical stand: Astra Linux 1.8 x86-64; currently installed test package is 0.2.6-rc1 and must not be reused
  as the public stable artifact.'
- Confirmed release base main commit is 04d5f9865b4e7e16c4e1828e239abb7635706ca1 and current public VERSION is 0.2.5.
- Implemented canonical 0.2.6 version bump across VERSION, runtime APP_VERSION, and Windows file/product metadata.
- Extended release.yml to require exact-main Windows P0, Windows installer, Linux Debian package, and Release Evidence Package
  before tag publication; public release set now includes Windows portable, Windows Setup, Astra Linux amd64 DEB, and SHA256SUMS.
- Added generic immutable GitVerse release mirror workflow with deterministic single-file ZIP wrappers for unsupported extensions
  such as .exe and .deb and post-download canonical payload SHA-256 verification.
- Extended exact-SHA release evidence to require Linux Debian CI and seal release/mirror workflow inputs.
- Updated README, RELEASE_POLICY, bilingual v0.2.6 release notes, and GitVerse mirror documentation for the Windows + Astra
  stable release.
- 'Local release/repository tests: 27 passed; version-policy tests: 5 passed; workflow YAML parsed successfully; git diff
  --check clean.'
current: Commit and push release/v0.2.6-final, open PR, and obtain exact-head CI.
pending:
- Push PR and obtain exact-head CI.
- Merge to main and verify exact-main release prerequisites.
- Download the exact-main Ubuntu 22.04 Debian artifact and physically validate it on Astra Linux 1.8.
- Create immutable tag v0.2.6 and verify GitHub release.
- Verify GitVerse mirror payloads and final published package on Astra.
- Close checkpoint with final evidence.
last_verified_commit: 04d5f9865b4e7e16c4e1828e239abb7635706ca1
test_results:
- python3 -m unittest tests.test_release_automation tests.test_release_evidence tests.test_repository_hygiene -q => 27 passed.
- /usr/bin/python3 -m unittest tests.test_version_policy -v => 5 passed.
- PyYAML parse of release.yml, release-evidence.yml and sync-release-to-gitverse.yml => PASS.
- python3 -m py_compile proxy_core.py => PASS; git diff --check => PASS.
updated_at: '2026-09-16T20:05:00Z'
resume_instruction: Push branch release/v0.2.6-final from /tmp/proxy-launcher-release-check, open PR, fix any exact-head CI
  failures, merge, wait for exact-main Windows/Linux/release-evidence gates, physically test the exact-main Ubuntu 22.04 DEB
  on Astra, then tag v0.2.6 and verify both GitHub and GitVerse releases. Do not publish rc1.
execution:
  claimed_by: chatgpt
  execution_id: APL-REL-017-20260916
  branch: release/v0.2.6-final
  base_commit: 04d5f9865b4e7e16c4e1828e239abb7635706ca1
  lease_until: '2026-09-16T21:35:00Z'
  manual_override: true
