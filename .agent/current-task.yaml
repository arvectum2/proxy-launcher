version: 2
repository: arvectum2/proxy-launcher
task_id: APL-MACOS-SLEEP-WAKE-LIVE-RECOVERY-20260922
status: in_progress
goal: "Make macOS APL remain usable across sleep/wake without requiring APL disable or a full browser restart, while preserving safe rollback and explicit user lifecycle actions."
authority: OPERATOR
definition_of_done:
  - "GUI wake/HID bugs no longer cause unintended --stop/rollback."
  - "Stored APL settings remain intact."
  - "After real sleep/wake, browser traffic remains usable through APL without disabling APL or relaunching the browser."
  - "Browser-session recovery is narrowly scoped to proven Safari/Chrome network subprocesses, preserves browser main processes/tabs, skips unrelated WebKit and browsers using --no-proxy-server, and does not mutate APL/system proxy state."
  - "Focused/full/static/build/CI checks show no new regressions."
completed:
  - "Strict macOS worker ownership recovery, PAC-health false STOPPED fix, wake/HID guard, synchronous destructive-handler guard, and deferred Off confirmation implemented."
  - "Latest manual OFF at 21:07:35-21:07:37 behaved correctly: confirmation_requested -> confirmed -> --stop; no false/synthetic stop."
  - "Immediately before that manual OFF, APL worker successfully opened fresh upstream CONNECT tunnels and received HTTP 200, yet browser connectivity was unusable; disabling APL and relaunching browser restored connectivity."
  - "Historical PR #149 isolation already disproved PAC refresh, manual-route refresh, full transport rebind, and forced pre-sleep relay retirement as sufficient."
  - "Historical stable-listener + legacy relay semantics also failed, narrowing remaining fault to browser/macOS client network-session state."
  - "Safari relaunched at 21:07:44 after recovery; Google Chrome main process stayed long-lived."
  - "Validated narrow Safari recovery: SIGKILL only the Safari-owned com.apple.WebKit.Networking process; Safari main PID and window/tab count remained unchanged, WebKit Networking relaunched with a new PID."
  - "Validated narrow Chrome recovery: SIGKILL only network.mojom.NetworkService; Chrome main PID remained unchanged and Chrome automatically relaunched both NetworkService and ProxyResolverFactory."
  - "Safari ownership can be proven fail-closed by lsof evidence under ~/Library/Containers/com.apple.Safari; another WebKit.Networking process belonged to Telegram and was excluded."
  - "Chromium-Gost is launched with --no-proxy-server and its NetworkService is explicitly excluded."
  - "Implemented macos_browser_network_recovery.py: exact current-user process matching, Safari container proof, exact Google Chrome parent proof, fail-closed SIGKILL and structured recovery logging."
  - "Implemented macOS worker resume detector using wall-clock versus monotonic suspend gap; on gap >=5s it recycles only proven browser network subprocesses, with 60s recovery coalescing. It does not touch APL listeners, transport, system proxy, PAC, or browser main processes."
  - "Live helper selector chose only Safari and Google Chrome; live helper execution recycled only those network services. Safari/Chrome/Chromium-Gost main PIDs stayed unchanged; Telegram WebKit and Chromium-Gost NetworkService stayed untouched."
  - "Focused suite PASS: 158 tests."
  - "Full suite: 1108 passed plus 3 historical unrelated Windows App Control failures."
  - "Changed-file Ruff F,E9,I PASS; git diff --check PASS."
current: "Commit browser-network resume recovery, build/install exact macOS candidate, push PR #155 and inspect exact-head CI. Then physical sleep/wake acceptance must test browser usability without APL OFF or browser relaunch."
pending:
  - "Commit code/tests/checkpoint."
  - "Governed macOS build/license/codesign and exact install."
  - "Enable APL and verify worker/listeners/system route/external proxy connectivity."
  - "Push/update PR #155 and inspect exact-head CI."
  - "Human real sleep/wake acceptance: browser must remain usable without disabling APL or relaunching browser; capture browser_network_recovery logs/PIDs."
last_verified_commit: "e3b1fad88b5d6fc26dacd4b1f871751df897bec6"
test_results:
  - "158 focused tests PASS."
  - "Full suite: 1108 passed, 3 historical unrelated Windows App Control failures."
  - "Changed-file Ruff F,E9,I PASS; git diff --check PASS."
  - "Live Safari network-service recycle PASS without closing Safari/tabs."
  - "Live Chrome NetworkService recycle PASS; NetworkService and ProxyResolverFactory relaunched while Chrome main stayed alive."
resume_instruction: "Do not revisit PAC/transport/system-proxy wake mutations. Build/install the current browser-network resume candidate. On the next real sleep/wake, inspect proxy.resume.browser_network_recovery phases and browser network-service PIDs before any manual recovery. PASS requires browser usability through APL with no APL OFF/browser relaunch."
updated_at: "2026-09-22T21:27:00+03:00"
execution:
  claimed_by: "chatgpt"
  execution_id: "interactive-20260922-macos-sleep-wake-live-recovery"
  branch: "agent/apl-macos-net-recovery-20260921"
  base_commit: "a148e20bb82bd41dc87d47d14afea8027db6c3cb"
  lease_until: null
  manual_override: true
