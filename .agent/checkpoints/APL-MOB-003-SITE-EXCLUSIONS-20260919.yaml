version: 2
repository: arvectum2/proxy-launcher
task_id: APL-MOB-003-SITE-EXCLUSIONS-20260919
status: in_progress
goal: "Add Android site/domain exclusions with the same user-facing purpose as desktop no_proxy, without starting per-application routing."
authority: HUMAN
definition_of_done:
  - "Android exposes an Exceptions editor without adding controls to the accepted one-screen home or adding per-app selection."
  - "Entries accept domain, URL, host:port and IP forms, are normalized, de-duplicated and synchronously persisted for the :vpn process."
  - "Configured supported exclusions bypass the Android VPN/proxy path while all other traffic remains tunneled."
  - "Empty exclusions preserve the accepted Android 0.1.17 networking path and tun2proxy virtual DNS behavior."
  - "Android 13+ uses VpnService excludeRoute; Android 8-12 uses a deterministic compatible route-complement implementation."
  - "When exclusions are active, DNS mode exposes real destination addresses required for IP route bypass instead of tun2proxy virtual addresses."
  - "Saving exclusions while connected reconfigures the tunnel automatically without requiring a second VPN permission grant."
  - "Unsupported patterns are rejected explicitly rather than silently providing incorrect bypass semantics."
  - "Version/docs/tests are updated; relevant local checks and exact-head Android CI pass."
  - "Checkpoint records final commit, verification and any physical-test requirement before completion."
completed:
  - "Synced local canonical checkout to origin/main at 8b07165a2f8404ed711399c6e65ec76a22b46a72."
  - "Verified desktop no_proxy normalization/matching/UI contract and current Android VpnService/tun2proxy architecture."
  - "Verified current Android uses tun2proxy --dns virtual, which returns synthetic destination IPs and therefore requires a DNS-mode change when IP route exclusions are active."
  - "Created isolated worktree/branch agent/apl-mob-003-site-exclusions-20260919 from current origin/main."
current: "Implement storage/normalization, Android split-route policy, tunnel DNS/reconfigure behavior, and the Exceptions editor."
pending:
  - "Implement and unit-test site-exclusion normalization/storage and bounded route generation."
  - "Integrate resolved exclusions into VpnService.Builder and tun2proxy DNS strategy."
  - "Add Exceptions entry/editor to the existing profile popup without changing the home layout."
  - "Add connected-save tunnel refresh, version 0.1.18 documentation and regression checks."
  - "Run local deterministic checks, push branch, open/update PR, and verify exact-head Android CI."
last_verified_commit: "8b07165a2f8404ed711399c6e65ec76a22b46a72"
test_results:
  - "Repository sync/worktree creation: PASS."
  - "Architecture review against Android VpnService and pinned tun2proxy v0.8.3: PASS."
resume_instruction: "Continue in /Volumes/ArvectumSSD/Arvectum/worktrees/apl-mobile-exclusions. Start with SiteExclusionPolicy + SecureProfileStore persistence and unit tests, then integrate VpnService route/DNS behavior and UI. Do not add per-app routing and do not alter empty-exclusion 0.1.17 network behavior."
updated_at: "2026-09-19T11:20:00+03:00"
execution:
  claimed_by: "ChatGPT"
  execution_id: "APL-MOB-003-SITE-EXCLUSIONS-20260919"
  branch: "agent/apl-mob-003-site-exclusions-20260919"
  base_commit: "8b07165a2f8404ed711399c6e65ec76a22b46a72"
  manual_override: true
