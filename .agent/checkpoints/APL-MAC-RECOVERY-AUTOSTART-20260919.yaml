version: 2
repository: arvectum2/proxy-launcher
task_id: APL-MAC-RECOVERY-AUTOSTART-20260919
status: completed
goal: "Repair macOS rollback/recovery and autostart so Launcher can safely restore the pre-Arvectum PAC state without severing the owner's proxy connectivity."
authority: OWNER_DIRECTIVE
definition_of_done:
  - "macOS rollback correctly restores services whose original PAC URL was empty/disabled without calling networksetup with an invalid empty URL."
  - "A second rollback attempt safely completes a partially restored state when each service is either still Arvectum-owned or already at its saved snapshot."
  - "macOS process ownership does not mistake a foreign compatible localhost PAC listener for the Arvectum engine."
  - "macOS autostart uses the dedicated ownership-safe LaunchAgent implementation and no Windows portable/stable-copy path."
  - "Relevant backend/GUI/process/autostart tests pass."
  - "Owner receives and executes an exact recovery command that restores the saved pre-Arvectum PAC state without losing RDC connectivity."
  - "Live enable/disable/restore acceptance passes with Arvectum on non-conflicting local ports."
completed:
  - "Recovered prior completed macOS UI checkpoint and verified repository/live host state."
  - "Read durable rollback evidence and identified the exact pre-Arvectum PAC/bypass snapshot."
  - "Fixed disabled+empty PAC restoration so networksetup is never called with an invalid empty PAC URL."
  - "Made rollback retry-safe after partial restoration while preserving fail-closed behavior for foreign changes."
  - "Fixed macOS process ownership so a foreign compatible localhost PAC listener cannot be mistaken for the Arvectum engine."
  - "Made macOS PID termination fail closed on executable identity mismatch."
  - "Replaced Windows portable/Run/schtasks autostart path on macOS with ownership-safe per-user LaunchAgent handling."
  - "Validated LaunchAgent enable/disable ownership protection with targeted and broader automated tests."
  - "Implementation committed and pushed as 943de63ac1b5c12c5c47c464c6287b8dbca9b8c1."
  - "Owner manually restored the saved baseline while retaining the baseline tunnel-proxy.py and RDC connectivity."
  - "Verified restored baseline: Ethernet/Wi-Fi PAC enabled on http://127.0.0.1:8082/proxy.pac; Thunderbolt Bridge/Tailscale PAC disabled; saved bypass domains restored."
  - "Cleared stale recovery evidence only after the fixed backend verified all services matched the saved snapshot."
  - "Separated Arvectum local ports from the owner's baseline proxy: HTTP 18080, SOCKS5 11080, PAC 18082; upstream credentials/settings were preserved."
  - "Installed the fixed private arm64 app in /Applications; strict deep codesign PASS; installed executable SHA256 b4dfe66fa1a4cf9476ec3f433356869440176765948b1983cabfa24f3c557d9c."
  - "Live enable acceptance PASS: Arvectum PID 90624 listened on 18080/11080/18082 and all four services switched to http://127.0.0.1:18082/proxy.pac with system proxy ENABLED."
  - "Live stop/restore acceptance PASS: Arvectum listeners closed, baseline tunnel-proxy.py remained alive on 8080/1080/8082, Ethernet/Wi-Fi restored to PAC 8082, Thunderbolt/Tailscale restored to PAC off, bypass domains restored and recovery evidence cleared."
  - "Exact GUI autostart handler acceptance PASS: enable -> disable -> enable; LaunchAgent plist lint PASS, mode 600, canonical /Applications executable, RunAtLoad=true."
  - "Autostart was intentionally left enabled because the Owner had requested the checkbox."
  - "Final GUI launch health PASS: installed Launcher window process stayed alive, CLI state STOPPED, baseline network state remained unchanged and recovery evidence remained clear."
current: "Completed. Fixed Launcher is installed and open in safe STOPPED state; baseline proxy remains active; autostart is enabled."
pending: []
last_verified_commit: "cd33e24"
test_results:
  - "Targeted backend/process/GUI/autostart automated tests: 42/42 PASS."
  - "Full test_macos*.py discovery: 45/45 PASS."
  - "Broader process/GUI/runtime/core/backend/license suite: 115/115 PASS."
  - "git diff --check PASS on implementation branch."
  - "Private arm64 app strict deep codesign PASS; bundle id ru.arvectum.proxylauncher."
  - "Live port-separation acceptance PASS: baseline 8080/1080/8082 and Arvectum 18080/11080/18082 coexist without collision."
  - "Live enable PASS; system proxy reported ENABLED on PAC 18082."
  - "Live stop/restore PASS; exact functional baseline restored and recovery snapshot cleared."
  - "Live autostart GUI-handler PASS; LaunchAgent enable/disable/reenable and plist validation succeeded."
  - "Final GUI launch PASS with baseline PAC unchanged."
resume_instruction: "Task is complete. Keep Arvectum local ports at 18080/11080/18082 while the owner's baseline tunnel-proxy.py owns 8080/1080/8082. The installed app is safe and currently STOPPED; autostart is enabled. Any further macOS visual/icon work should start a separate task and must preserve this verified recovery behavior."
updated_at: "2026-09-19T02:58:00+03:00"
execution:
  claimed_by: "ChatGPT"
  execution_id: "APL-MAC-RECOVERY-AUTOSTART-20260919"
  branch: "agent/macos-recovery-autostart-20260919"
  base_commit: "87c14fe"
  implementation_commit: "943de63ac1b5c12c5c47c464c6287b8dbca9b8c1"
  pre_completion_checkpoint_commit: "cd33e24"
  manual_override: true
