version: 1
repository: arvectum2/proxy-launcher
task_id: APL-SAFARI-HYBRID-PROXY-20260923
status: in_progress
goal: "Make Safari reliably load ChatGPT while APL is enabled, without breaking Chrome, Telegram, or rollback."
authority: OPERATOR
definition_of_done:
  - "Safari loads ChatGPT with APL enabled."
  - "macOS system route uses the configured upstream directly rather than a localhost relay."
  - "Telegram remains connected through legacy 127.0.0.1:1080."
  - "APL Off restores the exact saved baseline without error."
  - "Direct-upstream macOS routing is covered by regression tests and full suite."
  - "Exact installed build passes physical APL-ON and Off acceptance."
completed:
  - "Baseline: direct system HTTP/HTTPS 68.209.60.141:8000 works in Safari; ChatGPT main document 200 with sustained 200/202 API and event-stream traffic."
  - "SOCKS-first APL reproduced Safari WebSocket failure: NSURLErrorDomain -1011."
  - "Hybrid local HTTP 18080 + SOCKS 11080 still used SOCKS for Safari and WebSocket failed -1011."
  - "HTTP-only local APL 18080 with fresh Safari produced ChatGPT main-resource HTTP 403."
  - "Legacy local tunnel-proxy 8080 with fresh Safari produced the same ChatGPT main-resource HTTP 403."
  - "Therefore the incompatibility is the localhost chained-proxy architecture itself, not APL's local relay implementation."
  - "Direct system upstream 68.209.60.141:8000 with explicit networksetup auth attempt physically loaded ChatGPT successfully in fresh Safari; main resource 200, APIs 200/202, event-stream 200."
  - "networksetup reported Keychain duplicate-item -25299 and Authenticated Proxy Enabled: 0 while the existing Keychain-backed direct proxy still worked; ownership therefore cannot depend on the reported auth flag."
  - "ProxyBackendConfig now carries transient upstream HTTP URL/username/password. Password is not persisted in rollback applied_config."
  - "system_proxy_runtime resolves the first configured upstream into direct macOS backend values."
  - "macOS enable now disables PAC/SOCKS and enables system HTTP/HTTPS directly at upstream host/port; bypass policy is preserved."
  - "macOS owned-state matching now checks direct enabled host/port and ignores unreliable networksetup auth flag."
  - "Rollback remains schema v2 snapshot/restore of HTTP/HTTPS/SOCKS/PAC/bypass."
  - "56 direct-backend/runtime contract tests PASS; Ruff F,E9,I and diff-check PASS."
current: "Commit direct-upstream macOS mode, run full suite, build/install exact candidate, then start APL without restarting the currently working Safari."
pending:
  - "Commit code/tests/checkpoint and push branch."
  - "Run full suite; accept only the historical unrelated Windows failures."
  - "Governed build/license/codesign and exact install while APL is OFF."
  - "Start APL direct-upstream and verify networksetup remains 68.209.60.141:8000, SOCKS/PAC off, bypass owned."
  - "Reload same Safari ChatGPT without browser restart; require main 200 and working API/event-stream."
  - "Verify Chrome/system connectivity and Telegram legacy 1080."
  - "Run physical Off rollback and then leave final candidate in desired working state."
last_verified_commit: "a7113b675a43404ecc1c84396b53b62be1668522"
test_results:
  - "56 profile tests PASS."
  - "Ruff F,E9,I PASS; git diff --check PASS."
  - "Physical direct-upstream Safari ChatGPT PASS."
resume_instruction: "Do not return to localhost SOCKS/HTTP routing for macOS Safari. Finish direct-upstream backend: full tests, exact build/install, APL-ON same-Safari acceptance, Off rollback. Keep legacy tunnel-proxy running for Telegram 1080."
updated_at: "2026-09-23T16:10:00+03:00"
execution:
  claimed_by: "chatgpt"
  execution_id: "interactive-20260923-safari-direct-upstream"
  branch: "agent/apl-macos-net-recovery-20260921"
  base_commit: "8e0aee5ea5e91b5f314570385e7b949da02b3a3a"
  lease_until: null
  manual_override: true
