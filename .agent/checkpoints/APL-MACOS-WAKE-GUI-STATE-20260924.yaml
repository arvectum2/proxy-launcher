version: 1
repository: arvectum2/proxy-launcher
task_id: APL-MACOS-WAKE-GUI-STATE-20260924
status: in_progress
goal: "Fix the post-sleep macOS FAIL where the GUI can show stale connected state with both On/Off buttons disabled, and determine whether the underlying worker/network actually fails during sleep or only the GUI/CFNetwork state does."
authority: OPERATOR
definition_of_done:
  - "Capture current post-sleep failure before any network mutation."
  - "Prove why both On and Off can remain disabled after wake guard expiry."
  - "Fix wake-guard expiry so GUI state self-reconciles without focus/click."
  - "Determine whether worker survives the failing sleep; if not, identify and fix the worker lifecycle cause."
  - "Install a new candidate based on current main/release code, not the stale 0.2.12 candidate."
  - "Physical sleep/wake acceptance: GUI state matches backend, Safari/ChatGPT works, buttons recover automatically, Off rollback remains safe."
  - "Run focused/full/static tests and merge via PR only after acceptance."
completed:
  - "Synthetic guard expiry PASS: proxy_gui.wake_guard.expired logged ~15 seconds later without focus/click; backend remained RUNNING with the same worker PID and CFNetwork/bypass state intact."
  - "Synthetic wake-gap acceptance PASS: GUI PID 32728 was SIGSTOPed for 7 seconds while worker/network stayed live; on resume wake_guard.armed logged gap_seconds=7.326."
  - "Post-install baseline: APL RUNNING, worker PID 32739 owns 18080/11080/18082, system HTTP/HTTPS 68.209.60.141:8000 enabled, system SOCKS/PAC disabled, bypass count 47, CFNetwork egress 68.209.60.141, Telegram remains ESTABLISHED on legacy 1080, Safari ChatGPT reload PASS."
  - "Replaced the stale installed 0.2.12 candidate while APL was STOPPED and baseline network healthy; restarted GUI from the new bundle and started APL successfully."
  - "Built exact candidate from 5ff9c847f2a51a594f5bfb704f14fb690a2273c3: version 0.2.13, installed executable SHA-256 8a1592e735694089cc9e29b1bfac2ce45e271e02fe23cf1c702e3ae911a7f19b; license bundle and strict codesign verification PASS."
  - "Ruff F,E9,I and git diff --check PASS."
  - "Full suite: 1125 passed, 3 historical unrelated Windows App Control failures."
  - "Expanded macOS/runtime/GUI/transport suite: 196 PASS."
  - "proxy_gui focused suite: 38 PASS."
  - "Added regression tests for automatic control reconciliation after guard expiry and safe re-arm when another event-loop gap occurs before the expiry callback."
  - "Blocked destructive actions now use the same guarded expiry reconciler instead of a raw delayed refresh_status callback."
  - "Implemented guarded wake-expiry reconciliation: heartbeat now schedules a status refresh for guard expiry; the expiry callback first checks for a newer event-loop gap and re-arms the guard if necessary before any destructive control can re-enable."
  - "Historical WebKit logs before the user Off no longer contain enough route detail to prove the underlying Safari path; no unsupported root-cause claim is made for that part of the FAIL."
  - "Captured post-sleep state before mutation at 2026-09-24 00:33 +0300."
  - "Installed /Applications build is still pre-release candidate 0.2.12, SHA-256 36c312a3a04cbf81ec1f27b975842679d438bccef4366b8238a6b0fbc885f6dd; public v0.2.13 DMG had been released but not installed on this MacBook."
  - "CLI status at capture: STOPPED; APL worker/listeners 18080/11080/18082 absent; GUI PID 23480 still alive since 20:19."
  - "System network at capture is restored/direct baseline: HTTP+HTTPS 68.209.60.141:8000 enabled, system SOCKS disabled, PAC disabled, bypass count 29; legacy tunnel 8080/1080/8082 remains alive."
  - "Screenshot/UI was stale: it still displayed 'Прокси подключён' while backend status was STOPPED."
  - "Wake log shows repeated long event-loop gaps; latest guard armed at 00:29:40 local after ~287.947 s gap."
  - "GUI code root cause for sticky disabled Off found: _ui_heartbeat() refreshes status when the wake guard is armed, which disables Off, but does not schedule a status refresh when the 15-second guard expires. If no focus/status event occurs, Off can remain disabled indefinitely. On is naturally disabled in the active view, producing the observed both-disabled state."
  - "At 00:30:19 GUI later requested Off confirmation; user confirmation logged at 00:30:21; GUI spawned --stop. Current Tk process is healthy/idle in mainloop, not globally frozen."
current: "Patched 0.2.13 candidate is installed and RUNNING; synthetic wake-gap/expiry acceptance passed. Await one real sleep/wake cycle before merge."
pending:
  - "Physical sleep/wake acceptance on installed 8a1592e7 candidate: after wake, wait at least 20 seconds; require guard arm+expiry, matching GUI/backend state, Safari ChatGPT usable, and buttons auto-recovered."
  - "If PASS, update checkpoint, verify PR exact-head CI, merge, and prepare next patch release because v0.2.13 is immutable."
  - "If FAIL, capture live state before any Off and continue from the exact failing layer."
last_verified_commit: "e66e6658bf345cf58aafd7519e4cfeb479361489"
test_results:
  - "38 proxy_gui tests PASS."
  - "196 expanded macOS/runtime/GUI/transport tests PASS."
  - "Full suite: 1125 passed, 3 historical unrelated Windows App Control failures."
  - "Ruff F,E9,I PASS; git diff --check PASS."
resume_instruction: "Installed candidate 8a1592e735694089cc9e29b1bfac2ce45e271e02fe23cf1c702e3ae911a7f19b is RUNNING. Perform one real sleep/wake cycle. After wake wait 20 seconds before judging button state; Off may intentionally be disabled during the 15-second guard but must auto-recover after proxy_gui.wake_guard.expired. If FAIL, do not touch APL; capture evidence first."
updated_at: "2026-09-24T00:49:00+03:00"
execution:
  claimed_by: "chatgpt"
  execution_id: "interactive-20260924-wake-gui-state"
  branch: "agent/macos-wake-gui-state-20260924"
  base_commit: "58f24b9aab568f63d9807819c5d453f807244f6b"
  lease_until: null
  manual_override: true
