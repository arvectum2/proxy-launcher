version: 2
repository: arvectum2/proxy-launcher
task_id: APL-MACOS-FULL-NETWORK-OUTAGE-20260923
status: in_progress
goal: "Restore MacBook connectivity and make APL Off/rollback incapable of leaving a dead localhost SOCKS route after benign disabled-PAC metadata drift."
authority: OPERATOR
definition_of_done:
  - "MacBook browser/system connectivity restored."
  - "Telegram connectivity restored through its required legacy SOCKS 127.0.0.1:1080."
  - "APL Off restores the exact saved baseline without error when a disabled PAC URL is normalized to null."
  - "True foreign proxy changes are refused before worker termination, preserving live connectivity."
  - "No dead localhost proxy/SOCKS/PAC endpoint remains configured after Off."
  - "Relevant focused/full/static/build/physical checks pass with no new regressions."
completed:
  - "Captured live outage before mutation: APL worker was already gone and ports 18080/11080/18082 closed, but macOS still had SOCKS 127.0.0.1:11080 enabled; CFNetwork failed with connection refused."
  - "Durable macos_proxy_backup.json remained intact and showed Wi-Fi baseline HTTP/HTTPS 68.209.60.141:8000 enabled, SOCKS disabled, PAC disabled with remembered URL 127.0.0.1:8082."
  - "Exact log root cause: --stop and subsequent --rollback both refused with 'Wi-Fi matches neither Arvectum-owned nor saved state'."
  - "Per-service diagnostic proved all services except Wi-Fi still matched APL-owned state. Wi-Fi differed only because disabled PAC URL had normalized from saved 127.0.0.1:8082 to null; SOCKS/bypass/manual endpoints were otherwise exactly APL-owned."
  - "Emergency restore executed directly from durable backup across all saved services. All four service snapshots verified before backup was cleared."
  - "Post-restore Wi-Fi baseline: HTTP/HTTPS 68.209.60.141:8000 enabled, SOCKS disabled, PAC 8082 disabled; APL STOPPED and APL ports closed. CFNetwork returned 68.209.60.141."
  - "Telegram remained unavailable until legacy com.user.tunnel-proxy was re-enabled. After bootstrap, Telegram immediately opened multiple ESTABLISHED connections to 127.0.0.1:1080. This proves Telegram depends on legacy SOCKS 1080 and that disabling the legacy daemon was incorrect for this MacBook."
  - "Patched macOS ownership semantics: remembered PAC URL is ignored while PAC is disabled; enabled PAC still requires exact URL."
  - "Added shared _restore_plan validation used by disable_preflight and disable."
  - "disable_preflight now validates full service ownership/recoverability before worker termination, in addition to saved localhost PAC availability. True foreign changes therefore preserve the running worker/network path."
  - "Regression tests added for today's exact case: saved disabled PAC URL -> live null URL -> preflight/disable succeeds and restores saved URL; foreign manual route -> preflight refuses while owned SOCKS remains live."
  - "Backend+runtime focused: 45 PASS."
  - "Expanded macOS/runtime/GUI/transport suite: 208 PASS."
  - "Full suite: 1117 passed plus the same 3 historical unrelated Windows App Control failures."
  - "Ruff F,E9,I and git diff --check PASS."
current: "Commit patched rollback ownership/preflight logic, build/install exact macOS candidate, then physically reproduce disabled-PAC-URL-null state and verify Off rollback with legacy Telegram SOCKS left running."
pending:
  - "Commit code/tests/checkpoint and push branch."
  - "Governed macOS build/license/codesign and exact install while APL remains OFF."
  - "Start APL with legacy tunnel-proxy still running; verify system CFNetwork via APL 11080 and Telegram remains on legacy 1080."
  - "Physically remove remembered PAC URL while PAC remains disabled to reproduce today's drift."
  - "Run Off and verify worker is not killed on unsafe preflight, and benign URL drift restores baseline successfully with no dead SOCKS."
  - "Run final status/connectivity checks; update PR #155 and checkpoint completed."
last_verified_commit: "da969e22a3c025d8b822c2aac4dccdfa12a02281"
test_results:
  - "45 backend+runtime tests PASS."
  - "208 expanded focused tests PASS."
  - "Full suite: 1117 passed, 3 historical unrelated Windows failures."
  - "Ruff F,E9,I PASS; git diff --check PASS."
  - "Emergency restore PASS; system CFNetwork restored."
  - "Telegram legacy SOCKS 1080 dependency physically confirmed."
resume_instruction: "Do not disable legacy tunnel-proxy; Telegram requires 127.0.0.1:1080. Build/install the rollback patch. Then reproduce disabled PAC URL normalization while APL is active and verify patched Off restores baseline without leaving dead 11080."
updated_at: "2026-09-23T13:25:00+03:00"
execution:
  claimed_by: "chatgpt"
  execution_id: "interactive-20260923-full-network-outage"
  branch: "agent/apl-macos-net-recovery-20260921"
  base_commit: "da969e22a3c025d8b822c2aac4dccdfa12a02281"
  lease_until: null
  manual_override: true
