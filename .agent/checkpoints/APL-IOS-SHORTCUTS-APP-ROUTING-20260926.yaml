version: 2
repository: arvectum2/proxy-launcher
task_id: APL-IOS-SHORTCUTS-APP-ROUTING-20260926
status: in_progress
goal: "Provide a consumer-iOS application-routing workaround using Shortcuts automations and APL App Intents for VPN connect/disconnect."
authority: OWNER
definition_of_done:
  - Add APL App Intents for connect and disconnect, usable from Shortcuts without foreground UI.
  - Add App Shortcuts metadata so APL actions are easy to discover.
  - Replace the dead-end consumer app-exclusions screen with a truthful automation setup UX.
  - Deep-link users into Shortcuts creation and explain Opened/Closed trigger setup.
  - Preserve the managed Per-App VPN capability path and existing site exclusions.
  - Run Swift tests, parse/build validation, CI, and merge if green.
  - Keep APL-UI-001 as a pre-release gate.
completed:
  - Confirmed Apple Shortcuts supports App Opened/Closed personal-automation triggers and immediate execution.
  - Confirmed App Intents can expose APL actions in Shortcuts.
  - Confirmed Apple provides shortcuts://create-shortcut deep link, but no public API for APL to silently create a Personal Automation trigger.
current: "APL App Intents and Shortcuts onboarding implemented; local parse/diff checks pass. Validate in CI and merge."
pending:
  - Validate full iOS build in GitHub Actions.
  - Merge PR if green and mark completed.
last_verified_commit: 8069048
test_results:
  - "git diff --check: PASS"
  - "xcrun swiftc -parse VPNShortcuts.swift VPNController.swift ContentView.swift: PASS"
  - "Local xcodebuild/swift test invocation stalled without compiler output; CI is authoritative for full build."
blocker: null
resume_instruction: "Continue on agent/ios-shortcuts-app-routing-20260926. App Intents and onboarding are implemented; push/validate CI, fix any compile issues, merge if green, then mark checkpoint completed. Do not claim APL can create Personal Automation triggers programmatically."
updated_at: "2026-09-26T14:10:00+03:00"
execution:
  claimed_by: chatgpt
  branch: agent/ios-shortcuts-app-routing-20260926
  base_commit: 8069048
  manual_override: true
