version: 2
repository: arvectum2/proxy-launcher
task_id: APL-WIN-020-FOREIGN-PROXY-OWNERSHIP
status: in_progress
goal: Make Windows active proxy rollback ownership-symmetric with Linux so newer foreign/admin proxy changes are never overwritten,
  then publish and physically accept a new immutable release on ARVECTUM-DEMO.
authority: HUMAN
definition_of_done:
- Reproduce the current Windows active foreign-change overwrite behavior with a controlled test and on ARVECTUM-DEMO without
  destroying user state.
- 'Add current-state ownership validation for WinINET and per-user proxy environment: each governed field may be original
  or Arvectum-applied; any third foreign value fails closed and preserves rollback evidence.'
- Keep the ordinary user-disables-Arvectum-PAC mixed state recoverable and idempotent.
- Add Windows regression tests covering restored, mixed, inverse-mixed, and foreign states plus existing orphan-PAC safety.
- Run focused and broader Windows/backend/release suites.
- 'Physically validate on ARVECTUM-DEMO: start -> external disable -> stop/restart; start -> foreign PAC/manual change ->
  stop refusal without overwrite -> safe cleanup/restoration.'
- Advance patch version without mutating v0.2.8, merge exact-head green PR, pass exact-main release gates, publish GitHub
  and GitVerse release, verify hashes/parity.
- Install exact public Windows release on ARVECTUM-DEMO, rerun physical regression, leave clean baseline, and mark checkpoint
  completed.
completed:
- 'Captured ARVECTUM-DEMO baseline without mutation: installed launcher 0.2.5; current WinINET manual proxy 68.209.60.141:8000;
  no rollback backup present.'
- Implemented saved-or-Arvectum ownership validation for every governed WinINET and per-user proxy environment field.
- 'Added atomic restore preflight: any foreign/third value or invalid pending backup refuses all Windows rollback before the
  first mutation and retains evidence.'
- Kept mixed/restored states retry-safe, including the ordinary case where the user manually disables the Arvectum PAC while
  other owned fields remain applied.
- Added regression coverage for WinINET mixed/inverse/foreign, proxy-env mixed/foreign, and atomic preflight behavior.
- Advanced canonical product version to 0.2.9 and added bilingual release/mirror documentation.
- 'Fixed two exact-head CI contract drifts: legacy ProxyOverride restore test now supplies an owned current WinINET snapshot;
  Windows FixedFileInfo tuple now matches 0.2.9.'
current: Push CI-contract fix and wait for clean exact-head PR checks; then download the exact-head Windows artifact for physical
  acceptance on ARVECTUM-DEMO.
pending:
- Open PR and pass exact-head CI.
- Install/run exact-head Windows candidate artifact on ARVECTUM-DEMO and validate mixed + foreign proxy scenarios physically.
- Merge exact-head candidate and pass exact-main release gates.
- Publish immutable v0.2.9 to GitHub/GitVerse and verify SHA/parity.
- Install exact public v0.2.9 on ARVECTUM-DEMO and rerun final acceptance.
- Finalize checkpoint as completed.
last_verified_commit: 4884f88077811272befb19c80d049421f9160c04
test_results:
- 'Focused Windows/backend ownership suite: 61/61 PASS.'
- 'Expanded Windows/backend/release non-Tk suite: 129/129 PASS.'
- Mac test_version_policy is locally blocked only by Homebrew Python missing _tkinter; version/core/version_info consistency
  checked directly and will run in CI/Windows.
- Python syntax checks PASS for windows_system_proxy.py, windows_backend.py and proxy_core.py.
- 'Post-CI-fix proxy_core + windows_system_proxy suite: 71/71 PASS; version_info FixedFileInfo 0.2.9.0 consistency PASS; git
  diff --check PASS.'
resume_instruction: 'Wait for PR #91 exact-head CI after the CI-contract fix. Once Windows portable/installer jobs are green,
  download exact-head artifact and run physical mixed/foreign rollback acceptance on ARVECTUM-DEMO before merge.'
updated_at: '2026-09-17T14:24:50.005293+00:00'
execution:
  claimed_by: chatgpt
  execution_id: APL-WIN-020-20260917
  branch: apl-win-foreign-state-fix
  base_commit: 4884f88077811272befb19c80d049421f9160c04
  lease_until: null
  manual_override: true
